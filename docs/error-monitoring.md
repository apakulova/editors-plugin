# Диагностика ошибок в аналитике

Документ описывает безопасную техническую диагностику ошибок для PostHog и Telegram-отчета. Базовая диагностика уже реализована: она помогает понять общую причину сбоя, внутреннюю операцию и область кода, но не отправляет пользовательский текст и не меняет отображение ошибки внутри Figma.

Дизайн пользовательского окна внутри Figma уже согласован в `preview/error-report-preview.html` и описан в `docs/error-ui-plan.md`. Перенос этого дизайна в рабочий плагин остаётся отдельной будущей задачей.

## Что уже реализовано

- `error_category` — понятная крупная причина ошибки;
- `error_operation` — внутренняя операция, на которой произошел сбой;
- `error_location` — стабильная область кода без полного stack trace;
- `error_fingerprint` — одинаковый технический отпечаток для одинаковых сообщений об ошибке;
- `error_name` — техническое имя ошибки;
- `stage` — крупный этап обработки: поиск слоёв, применение правил, загрузка шрифтов, чтение оформления, сравнение, запись, проверка оформления или аварийный возврат;
- `run_id` — случайный номер запуска, который связывает начало и результат без идентификации пользователя.

Ежедневный Telegram-отчет показывает количество неудачных попыток, число затронутых пользователей, долю ошибок и основную категорию причины. Более подробный разбор остается в PostHog.

Если содержимое текста изменилось во время подготовки записи, запуск получает техническую категорию `layer_changed`, а внутри Figma показывается согласованное уведомление `Тут изменился текст — запустите типограф заново 🔄`. Сам изменённый текст в аналитику не отправляется. Слой, который Figma пометила как удалённый до начала записи, безопасно пропускается и сам по себе не считается ошибкой запуска.

Если Figma не ответила за 15 секунд при загрузке страницы, шрифта или связи экземпляра с главным компонентом, запуск получает категорию `timeout` и завершается обычной ошибкой вместо бесконечного уведомления о работе. В сохранённом старом режиме полной записи такое же ограничение действует для загрузки библиотечного стиля или переменной. Сообщения `Style does not exist` и `Variable does not exist` больше не относятся к категории `layer_changed`: на этапе проверки оформления они записываются как `restore_styles_failed`.

Это внутренняя аналитика для автора плагина. Будущее пользовательское окно решает другую задачу: только внутри Figma можно безопасно показать конкретный проблемный слой и помочь перейти к нему. Текст, название и путь слоя во внешнюю аналитику не отправляются.

## Приватность

В аналитику нельзя отправлять:

- текст макета;
- названия слоев;
- названия файлов;
- названия страниц;
- email;
- имя пользователя;
- Figma user id;
- полный stack trace.

Отправляются только безопасные технические свойства: категория, операция, область кода, этап, fingerprint, счетчики и агрегаты. Исходное сообщение об ошибке используется внутри плагина для выбора категории и создания fingerprint, но само сообщение в PostHog не отправляется.

## Возможное дальнейшее расширение

Если текущих данных окажется недостаточно, событие `plugin_run_failed` можно будет дополнить более подробными счетчиками:

- `error_category_label` — редакторское название крупного типа;
- `first_failed_text_node_stage_label` — редакторское название этапа;
- `failed_text_node_stage_counts` — счетчики упавших текстовых слоев по этапам;
- `typography_rule_stage` — код правила типографики, если ошибка произошла внутри правил;
- `typography_rule_stage_label` — редакторское название правила.

В Telegram-отчете это могло бы выглядеть так:

```text
Типы ошибок:
— Обработка текстового слоя (text_node_processing): 1
— Поиск текстовых слоёв (collect_nodes): 0
— Неизвестная ошибка (unknown): 0

Где ломались текстовые слои:
— Восстановление оформления (restore_styles): 1

Правило типографики:
— Кавычки и пунктуация (quotes_punctuation): 1
```

Блок `Правило типографики` нужен только для ошибок этапа `clean_text`.

## Согласованные формулировки

### collect_nodes

Название: `Поиск текстовых слоёв`

Что значит: `Плагин сломался ещё до обработки текста — не получилось собрать все текстовые слои при обходе выбранного фрейма, страницы, заблокированных или скрытых слоёв.`

### unknown

Название: `Неизвестная ошибка`

Что значит: `мы не предусмотрели такой тип ошибки`

### read_text

Название: `Чтение текста`

Что значит: `Плагин нашёл сам текстовый слой, но почему-то не смог получить текст. Такое бывает из-за сбоев или когда макет внезапно поменялся.`

### read_development_markers

Название: `Поиск служебных звёздочек`

Что значит: `Плагин не смог проверить, какие звёздочки в режиме «Для разработки» были служебными, а какие пользовательскими.`

### clean_text

Название: `Применение правил типографики`

Что значит: `Плагин получил текст, но сломался во время применения правил типографики: кавычек, тире, пробелов, чисел или других замен.`

## Черновые коды для будущего согласования

Эти формулировки еще не согласованы. Их нужно пройти отдельно перед внедрением в UI, Telegram или PostHog.

- `text_node_processing` — крупная категория ошибки при обработке конкретного текстового слоя.
- `load_fonts` — проблема на этапе загрузки шрифтов перед изменением текста.
- `capture_styles` — проблема при сохранении оформления перед заменой текста.
- `build_style_map` — проблема при сопоставлении старого и нового текста для восстановления mixed styles.
- `write_text` — проблема при точечной вставке, удалении или замене символов.
- `restore_styles` — в точечной версии проблема при проверке сохранности оформления или компонентной связи; в старом режиме полной записи — проблема при возвращении шрифтов, заливок, списков, отступов или абзацных интервалов.
- `check_development_marker_styles` — проблема при проверке окраски служебных звездочек.
- `apply_development_marker_styles` — проблема при окрашивании служебных звездочек.
- `sync_development_marker_data` — проблема при сохранении или очистке plugin data для режима `Для разработки`.

## Идея разметки правил типографики

Если ошибка происходит внутри `clean_text`, в будущем можно дополнительно фиксировать конкретное правило. Это должна быть только диагностическая обертка вокруг существующего порядка правил.

Критически важно: такая разметка не должна менять сами правила типографики, порядок их применения или ожидаемые результаты тестов.

Черновой список кодов:

- `cleanup_spaces` — обычные пробелы;
- `quotes_punctuation` — кавычки и пунктуация;
- `math_symbols_first_pass` — математика и символы до тире/чисел;
- `dashes_hyphens` — тире и дефисы;
- `phone_numbers` — телефоны;
- `numbers_money` — числа и деньги;
- `abbreviations` — сокращения;
- `non_breaking_spaces` — неразрывные пробелы;
- `math_symbols_second_pass` — математика и символы после неразрывных пробелов;
- `spaced_years` — сборка разбитых годов.

Перед внедрением этот список нужно согласовать и проверить, что он точно соответствует текущему порядку `cleanTypography`.
