# Журнал коммитов

Этот файл хранит рабочую память проекта между чатами. После каждого нового локального коммита нужно добавлять сюда запись на русском языке: код коммита, что изменилось, зачем это было сделано, какие файлы затронуты, какие проверки прошли и что важно не потерять в следующем чате.

README должен оставаться коротким: полный журнал ведется здесь, а в README остается только ссылка на этот файл.

- `5f83ee6` — `Убрать ошибки из превью пробелов`.
  Из общей страницы `preview/nbsp-review-preview.html` полностью удалён добавленный для сравнения экран ошибок вместе с его стилями, разметкой и сценариями. Страница снова показывает один центрированный интерактивный экран проверки удалённых неразрывных пробелов. Отдельный эталон `preview/error-report-preview.html` и рабочий экран ошибок не менялись.
  В README, плане ошибок и дизайн-системе явно закреплено, что состояния `Не всё удалось обработать` и `Типограф остановил работу` используют один рабочий экран и одинаковую механику списка, строк, стрелок, прокрутки, сокращения путей, закреплённой кнопки, градиента и перехода с масштабированием к слою в Figma. Критическая остановка меняет только содержание верхнего блока, счётчика и групп. Добавлена автоматическая проверка общей реализации.
  Проверки: полный `npm test`, разбор сценария предпросмотра, `git diff --check` и визуальная проверка локальной страницы. Подтверждены один экран, отсутствие блока ошибок, работа выбора слоя и раскрытия длинного текста. Правила типографики, пользовательские тексты плагина, уведомления, аналитика и обработка слоёв не менялись.

- `443963d` — `Зафиксировать кнопку экрана проверки`.
  Для будущего экрана проверки удалённых неразрывных пробелов закреплена симметрия с рабочим экраном ошибок. После запуска из настроек кнопка `Проверить` переключает уже открытое окно на отчёт; внизу остаётся закреплённая кнопка `Вернуться к типографу`, которая возвращает в настройки с сохранёнными режимом и галочками. После быстрого запуска отчёт открывается без нижней кнопки, белой зоны и её градиента, а список использует освободившуюся высоту.
  Нерешённый вопрос о кнопке после быстрого запуска удалён из плана, оба сценария добавлены в обязательные ручные проверки. Изменены только `docs/nbsp-review-plan.md` и `docs/figma-regression-checklist.md`. Код, предпросмотр, видимые тексты и правила типографики не менялись. Проверка: `git diff --check`.

- `0cdfd68` — `Прокручивать слой только выше градиента`.
  Отменена слишком активная прокрутка выбранной строки к середине списка. Теперь рабочий экран проблемных слоёв двигается только при необходимости: уже видимая выше градиента строка остаётся на месте, перекрытая строка поднимается ровно на недостающее расстояние, а строка выше текущей области подводится к верхнему краю. Для последнего слоя список доходит до конца и градиент исчезает.
  Эталон ошибок и сравнительное превью используют то же поведение. На будущем экране проверки неразрывных пробелов оно действует также после раскрытия длинного текста. Планы обоих экранов, дизайн-система и ручная памятка Figma синхронизированы. Отступы, размеры, стили, видимые тексты, правила типографики, уведомления, аналитика, `manifest.json`, `src/code.ts` и `dist/code.js` не менялись.
  Проверки: полный `npm test`, разбор сценариев трёх интерфейсных файлов, автоматический запрет прокрутки к середине, `git diff --check` и визуальная проверка в локальном браузере. При выборе уже видимой второй строки прокрутка осталась на нуле; третья строка поднялась только на 11 пикселей до границы градиента; последний слой дошёл до конца списка, после чего градиент исчез.

- `1aba14b` — `Не перекрывать выбранный слой градиентом`.
  В рабочем экране проблемных слоёв выбранная строка теперь прокручивается к середине видимой области списка, а не только к ближайшему краю. Благодаря этому её текст и путь не попадают под белый градиент над закреплённой кнопкой. Для последнего слоя список доходит до конца и градиент исчезает. Одинаковая логика действует при клике и переходе стрелками.
  Эталон ошибок и сравнительное превью синхронизированы с рабочим экраном. На будущем экране проверки неразрывных пробелов то же поведение применяется также после раскрытия длинного текста. Планы обоих экранов, дизайн-система и ручная памятка Figma обновлены. Видимые тексты, правила типографики, уведомления, аналитика, `manifest.json`, `src/code.ts` и `dist/code.js` не менялись.
  Проверки: полный `npm test`, разбор сценариев трёх интерфейсных файлов, автоматический контроль прокрутки к середине, `git diff --check` и визуальная проверка в локальном браузере. Для третьего слоя подтверждён запас над градиентом; для последнего — полная прокрутка и исчезновение градиента.

- `187523e` — `Показывать выбранный слой в списке`.
  В рабочем экране проблемных слоёв клик по строке теперь прокручивает внутренний список так же, как стрелки: выбранная строка целиком оказывается в видимой области над закреплённой кнопкой. Переход к соответствующему слою в макете Figma сохранён. Эталон ошибок и будущий экран проверки неразрывных пробелов используют ту же механику; на экране пробелов она также работает при выборе строки с клавиатуры.
  Документация, дизайн-система, ручная памятка Figma и автоматическая проверка синхронизированы. Правила типографики, уведомления, аналитика и сборка ядра не менялись. Проверки: полный `npm test`, проверка сценариев интерфейсных файлов и `git diff --check`.

- `9c5ec45` — `Сохранять конец длинного пути слоя`.
  В рабочем экране проблемных слоёв длинный путь теперь сокращается по целым начальным уровням слева. Наиболее полезный конец с названием текстового слоя сохраняется: `frame / section / card / caption` превращается в `… / section / card / caption`. Ширина проверяется после открытия окна и пересчитывается при изменении его размера. Полный путь при наведении не показывается.
  Отдельный эталон ошибок и сравнительное превью используют ту же механику. В будущем экране проверки неразрывных пробелов поясняющая плашка перенесена внутрь прокрутки и уходит вверх вместе со слоями, тогда как общий счётчик и стрелки остаются закреплены сверху. Документация и ручная памятка Figma синхронизированы.
  Правила типографики, уведомления, аналитика, `manifest.json`, `src/code.ts` и `dist/code.js` не менялись. Проверки: полный `npm test`, проверка сценариев трёх интерфейсных файлов, автоматический контроль удаления начальных уровней пути и `git diff --check`.

- `528d980` — `Закрепить кнопку в экране ошибок`.
  В рабочем окне проблемных слоёв кнопка `Вернуться к типографу` вынесена из прокручиваемого списка в отдельную закреплённую белую зону. Пока ниже остаются скрытые слои, над зоной показывается плавный градиент высотой 18 пикселей; в конце прокрутки и при полностью видимом списке он исчезает. Последний слой можно полностью прокрутить выше кнопки. После быстрого запуска кнопка по-прежнему отсутствует, а состояние ошибки до начала обработки не менялось.
  Отдельный эталон ошибок синхронизирован с рабочим экраном. В общем интерактивном превью сохранены согласованный экран проверки неразрывных пробелов, новая иллюстрация, сравнение с экраном из семи проблемных слоёв, счётчики возле путей, ссылки раскрытия с количеством пробелов и одинаковая закреплённая кнопка на обоих экранах.
  В дизайн-системе закреплено общее продуктовое правило: на любом будущем экране с прокруткой и нижней кнопкой закрепляется вся белая зона с кнопкой, прокручивается только содержимое, а градиент показывает оставшийся ниже контент. README, планы экранов ошибок и неразрывных пробелов и ручная памятка Figma обновлены. Правила типографики, уведомления, аналитика, `manifest.json`, `src/code.ts` и `dist/code.js` не менялись.
  Проверки: полный `npm test`, повторная проверка сценариев в `src/ui.html` и обоих интерактивных превью, автоматический контроль отдельной строки для закреплённой кнопки и условного градиента, `git diff --check`.

- `d327fcc` — `Показывать подчёркивание вкладки поверх списка`.
  Подчёркивание активной вкладки поднято над прокручиваемым содержимым, поэтому при прокрутке вкладки `Правила` линия больше не обрезается и остаётся полностью видимой. Положение, размеры и отступы вкладок и линии не менялись.
  Затронуты только `src/ui.html` и автоматическая проверка в `scripts/test-clean-typography.js`. Тексты интерфейса, правила типографики, обработка текста, настройки запуска и аналитика не менялись.
  Проверки: полный `npm test`, `git diff --check` и ручная проверка вкладки `Правила` в локальном окне с прокруткой. В Figma после обновления нужно открыть вкладку `Правила`, прокрутить список вниз и убедиться, что зелёное подчёркивание активной вкладки остаётся видимым полностью.

- `4164069` — `Сохранять пробел перед смайликами`.
  Для сочетаний `:)`, `;)`, `:-)` и `;-)` добавлено исключение из общего удаления пробела перед двоеточием и точкой с запятой. Теперь плагин сохраняет авторский вариант: `посмеялся:)` остаётся слитным, а `посмеялся :)` сохраняет пробел. Обычное двоеточие продолжает приклеиваться к предыдущему слову. Дополнительно защищён поздний этап неразрывных пробелов: короткая фраза `я :)` сохраняет обычный пробел и в режиме `Для красоты`, и в режиме `Для разработки`.
  Во вкладку `Правила` добавлена заранее согласованная формулировка `Пробел перед смайликами сохраняется таким, как его поставил автор: посмеялся:) или посмеялся :-)`. README и подробный справочник правил обновлены одновременно. Постоянный код `space_before_punctuation` сохранён: это исключение существующего правила, а не новое самостоятельное преобразование текста.
  Затронуты `src/code.ts`, собранный `dist/code.js`, `scripts/test-clean-typography.js`, `src/ui-content.js`, сгенерированный `src/ui.html`, README и `docs/typography-rules.md`. Уведомления, настройки запуска, числовые правила, аналитика, Telegram/Vercel и визуальные стили интерфейса не менялись.
  Проверки: полный `npm test`, повторный запуск каждого примера, точечная запись, оба режима, обычное двоеточие и `git diff --check`. В Figma нужно проверить слитные и раздельные варианты всех четырёх смайликов, пример `я :-)` и обычную фразу `слово : значение`, которая должна стать `слово: значение`.

- `21fdbf1` — `Добавить сообщения о выпуске в меню`.
  В плагин добавлен опциональный пункт после системного разделителя: `⚠️ Не все числа делятся на разряды →`. Он открывает отдельный экран в общем окне Чистовика с утверждённой иллюстрацией, заголовком `Чистовик больше не делит все числа по разрядам`, оттипографированным пояснением, примерами и основной кнопкой `Вернуться к типографу`.
  Сообщения хранятся в `src/release-announcements.js`. Одновременно может быть активно одно сообщение; `activeId: null` убирает его и разделитель из следующей сборки, сохраняя архив. Поддерживаются одна или две кнопки, обязательный возврат к типографу, переход к правилам, основная зелёная и вторичная салатовая кнопка с границей. Если Figma сохранила выключенную команду в недавних действиях, она безопасно открывает вкладку `Типограф`, а не пустой экран.
  Сохранены три варианта иллюстрации в `assets/announcements`, локальный макет `preview/announcement-preview.html`, точный архив содержания и инструкция по добавлению, выключению и возврату сообщений. README, UI-дизайн-система и ручной список проверки синхронизированы. Сборка сама обновляет `manifest.json`, вставляет активный экран в `src/ui.html` и вкладывает картинку внутрь плагина без загрузки из интернета.
  Автоматические тесты проверяют включённое и выключенное состояния. Перед коммитом отдельно проверены обе сборки, затем точный состав коммита прошёл полный `npm test` и `git diff --cached --check`. Параллельные незакоммиченные изменения диагностики чисел намеренно не включались и были возвращены в рабочую папку после коммита.

- `43a9621` — `Уточнить контекст диагностики чисел`.
  Временная диагностика чисел поднята до сборки `4`. Дополнительный поиск соседнего текста теперь получает отдельный набор видимых слоёв из области запуска: внутри выделенного контейнера он не выходит за выделение, несколько выбранных контейнеров не передают контекст друг другу, а видимая заблокированная подпись может попасть в отчёт, даже если сам типограф её не обрабатывает. При запуске без выделения поиск по координатам ограничен близкими слоями с достаточно близким общим контейнером. Контекст, действительно участвовавший в числовом решении, по-прежнему показывается отдельно от общего соседа для ручной проверки.
  Диагностика больше не выдаёт изменение пробела или другого текста около числа за необъяснённое числовое изменение. Карточка остаётся в общей группе «Изменено типографом», но получает подпись «Изменён текст рядом», прямое объяснение, что числовое правило не применялось, и фильтруемый код `number_context_nbsp` или `number_context_change`. Экспорт сохраняет эти коды вместе с исходными символами пробелов. Диагностическая классификация отдельно узнаёт `MCC 5411` как MCC-код; валюта и другие доказательные признаки количества сохраняют более высокий приоритет.
  Номер диагностической сборки изменён на `4`, а версия числовых правил намеренно оставлена `numbers-2026-08-25-v1`: алгоритм типографики, вкладка `Правила`, пользовательские уведомления и интерфейс Figma-плагина не менялись. Серверный приём разрешает четвёртую версию схемы. Закрытая страница, демонстрационные данные, документация и проверки синхронизированы.
  Проверки: полный `npm test`, TypeScript-сборка, тест области одного и нескольких выделенных контейнеров, тест общего соседнего текста, тест изменения только пробела рядом с числом, тест MCC с сохранением приоритета валюты, тест приёма схемы `4`, тест 4 000 слоёв, `git diff --check` и визуальная проверка закрытой страницы в локальном браузере. Перед обновлением плагина в Figma серверная часть должна развернуться после push, чтобы сразу принимать пакеты сборки `4`; при временном несовпадении версий пакет останется в локальной очереди до следующего запуска.

- `82d6dd2` — `Дополнить контекст диагностики чисел`.
  Временная диагностика чисел поднята до сборки `3`. Для отдельного числового слоя она теперь ищет не только специальные подписи `ID`, `Счёт`, `Карта` и `Код`, но и ближайший обычный поясняющий текст со словом длиной не меньше четырёх букв в той же визуальной строке. Учитываются координаты, расстояние и общий предок; текст следующей строки не подхватывается. Такой сосед сохраняется только для ручной проверки и не становится новым признаком количества, поэтому версия числовых правил остаётся `numbers-2026-08-25-v1`.
  Закрытая страница показывает использованный алгоритмом слой как «Контекст решения», обычный поясняющий сосед — как «Соседний слой для проверки», а при реальном отсутствии подходящего контекста прямо сообщает «Соседний текстовый слой не найден». Жёлтое выделение изменившегося диапазона сохранено. Неразрывные пробелы внутри него продолжают обозначаться бордовыми звёздочками.
  Исправлен отдельный дефект режима «Для разработки»: его служебные звёздочки до диагностического сопоставления переводятся обратно в смысловые неразрывные пробелы. Поэтому `20 000` и показанное в режиме разработки `20*000` считаются одним числом, разрядная группа не распадается на `20` и `000`, а лишние карточки с пустым полем «До» или «После» больше не создаются. Это преобразование действует только в диагностической копии текста и не меняет слой в Figma.
  Серверный приём разрешает третью версию схемы, демонстрационные данные показывают найденный и отсутствующий контекст, README и документы числовой политики и диагностики обновлены. Затронуты `src/code.ts`, собранный `dist/code.js`, закрытая страница в `public`, серверная проверка пакетов, тесты и документация. Вкладка `Правила`, пользовательские уведомления и результат типографики не менялись. Перед публикацией плагина серверная часть должна успеть развернуться после push, иначе прежний сервер временно отклонит пакеты третьей версии и они останутся в локальной очереди до следующего запуска.
  Проверки: `npm test`, TypeScript-сборка, все тесты типографики, аналитики, ежедневной доставки и числовой диагностики, `git diff --check`, тест общего соседнего текста и границы следующей строки, тест 4 000 слоёв, сопоставление служебных звёздочек в обе стороны, отсутствие пустых полей и ручная проверка закрытой страницы в браузере.

- `1b07ee5` — `Улучшить диагностику аварийного возврата`.
  При критической ошибке восстановления текстового слоя постоянная аналитика теперь раздельно сохраняет безопасное описание исходной ошибки и точный этап, на котором не удалось вернуть слой. Дополнительно фиксируются внутренняя операция возврата, причина отказа, безопасный отпечаток ошибки и перечень частей снимка, которые не прошли финальную проверку: текст, оформление, служебные метки режима разработки, связи с компонентными свойствами или цепочка родителей. Пользовательский текст, названия и номера слоёв в PostHog не отправляются.
  Схема постоянной аналитики поднята до `14`; серверный разрешённый список и тесты синхронизированы. Сам порядок восстановления и правило остановки при неподтверждённой целостности сохранены: изменение добавляет подробности для разбора редкой аварии, но не ослабляет защиту и не добавляет пользователю новых уведомлений. Затронуты `src/code.ts`, собранный `dist/code.js`, серверная проверка аналитики, тесты, `docs/error-monitoring.md` и `docs/plugin-analytics.md`.
  Проверки: `npm test`, тест тайм-аута возврата, тест несовпадения оформления и компонентных связей, проверка разрешённых значений сервером и автоматические тесты GitHub на Node.js 20 и 22.

- `f0c8f2d` — `Актуализировать документацию числовой политики`.
  Основной документ о доказательной обработке чисел переименован из `docs/number-grouping-policy-plan.md` в `docs/number-grouping-policy.md`: это больше не план будущей доработки, а подробное описание уже выпущенной функции. В документе зафиксированы коммит реализации `00979f7`, действующая версия числовых правил `numbers-2026-08-25-v1`, реализованный каталог признаков и защит, порядок принятия решений, аналитические коды, состав выпущенного изменения, матрица тестов и статус исходных критериев готовности. Исторические записи аудита сохранены, а оставшиеся пункты явно отделены от смысловой готовности функции.
  README синхронизирован с фактическим порядком обработки чисел и телефонов, правилами приоритета валюты и статусом опубликованной приватной бета-версии. Исправлен устаревший пример: голое `10000 клиентов` не получает разрядные пробелы, потому что обычное существительное без поддерживаемого сокращения не является достаточным признаком количества. Добавлена ссылка на полный документ числовой политики.
  План развития приведён к текущему состоянию: точечная обработка и доказательная числовая политика отмечены как выпущенные, текущим этапом назван сбор временной диагностики до 18 сентября 2026 года. Внутренний справочник правил, описание диагностики и общей аналитики получили взаимные ссылки и актуальные версии. Расписание ежедневного отчёта исправлено на основной запуск GitHub Actions в `09:17` по Москве и резервное окно Vercel с `10:00` до `10:59`. В регрессионный список Figma добавлена отдельная глубокая проверка голых чисел, количеств, дробей, переноса валюты, неоднозначных списков и диапазонов, строгих границ между слоями, оформления и повторного запуска.
  Затронуты только `README.md` и документы в `docs`. Код Figma-плагина, собранный `dist/code.js`, серверные обработчики, правила типографики, вкладка `Правила`, интерфейс и пользовательские уведомления не менялись. Обновлять или заново публиковать плагин в Figma не нужно.
  Проверки: полный `npm test`, TypeScript-сборка, все тесты типографики, аналитики, ежедневной доставки и числовой диагностики, `git diff --check`, поиск устаревшего имени документа и статусов, сверка версий числовой аналитики, порядка обработки и расписаний с кодом, workflow GitHub и `vercel.json`.

- `685e7b0` — `Защитить ежедневный отчёт от пропусков и дублей`.
  Основной ежедневный Telegram-отчёт перенесён на GitHub Actions с расписанием `06:17 UTC`, то есть `09:17` по Москве. Запуск стоит не в начале часа, чтобы уменьшить риск задержки или пропуска при высокой нагрузке GitHub. Vercel Cron перенесён на `07:00 UTC` и работает как независимая страховка; на тарифе Hobby это окно с `10:00` до `10:59` по Москве. Ручной запуск GitHub сохранён.
  Добавлена общая защита доставки через таблицу `daily_analytics_report_deliveries` в уже подключённой базе Neon. На одну московскую дату хранится только служебное состояние, источник, случайный номер права на отправку, количество попыток, время и безопасная краткая ошибка. Текст отчёта, данные макетов и секреты в таблицу не попадают. Первая попытка получает право отправки; состояния `sent` и `sending` блокируют обычный повтор, `failed` разрешает страховке повторить попытку, а `uncertain` запрещает автоматический повтор, если сетевой ответ Telegram потерян и нельзя доказать, было ли сообщение доставлено. Принудительный повтор доступен только отдельным выключенным по умолчанию флажком ручного workflow.
  Общая логика вынесена в `scripts/lib/daily-report-delivery.js`, работа с состоянием — в `scripts/lib/daily-report-delivery-store.js`. И Vercel endpoint, и GitHub-скрипт используют один путь. Telegram-сеть помечает неопределённый результат отдельно; подтверждение более старой попытки не может перезаписать право новой. После окончания временной диагностики чисел удаляется только таблица подробных числовых случаев, служебная история доставки отчётов сохраняется.
  В `.github/workflows/daily-analytics.yml` добавлены ежедневное расписание, ограничение параллельности, время ожидания, источник запуска и ручной принудительный повтор. `vercel.json` переведён на запасное часовое окно. README и `docs/plugin-analytics.md` фиксируют архитектуру, времена, секреты, состояния, порядок ручной проверки и диагностику пропуска. Добавлен отдельный тест `scripts/test-daily-report-delivery.js`, включённый в `npm test`.
  Проверки: синтаксис новых серверных модулей, точечные тесты доставки, полный `npm test`, сборка TypeScript, все тесты типографики, аналитики и числовой диагностики, `git diff --check`. На настоящей базе отдельной тестовой датой проверены первое право отправки, блокировка одновременной попытки, повтор после явной ошибки и блокировка после `sent`; тестовая строка затем удалена. `src/code.ts`, `dist/code.js`, `manifest.json`, интерфейс, правила типографики и пользовательские уведомления не менялись; обновлять Figma-плагин не нужно.

- `4f73a2b` — `Исправить резервную отправку ежедневного отчёта`.
  После добавления временной диагностики чисел ручной workflow `Daily analytics report` перестал запускаться на чистом GitHub runner: общий модуль отчёта загружал `@neondatabase/serverless`, но workflow не устанавливал зависимости проекта. Ошибка происходила до обращения к Telegram, поэтому неудачный запуск не создавал частично отправленного сообщения или дубля.
  В `.github/workflows/daily-analytics.yml` добавлена установка рабочих зависимостей через `npm ci --omit=dev --ignore-scripts` и кеш npm. В GitHub Actions добавлен закрытый секрет `NUMBER_DIAGNOSTICS_DATABASE_URL`, а workflow передаёт его отчёту, чтобы ручной резерв показывал то же количество числовых случаев, что и основной запуск Vercel. `docs/plugin-analytics.md` дополнен полным списком секретов, обязательной установкой зависимостей и проверкой зелёного статуса перед поиском сообщения в Telegram.
  Первый диагностический ручной запуск остановился на отсутствующей библиотеке до отправки. После исправления повторный запуск успешно прошёл шаги установки зависимостей, формирования и отправки отчёта за 25 августа 2026 года. Изменение относится только к внутреннему Telegram-отчёту и GitHub Actions; код Figma-плагина, правила типографики, интерфейс и пользовательские уведомления не менялись.
  Проверки: наличие закрытого секрета по имени без чтения его значения, `git diff --check`, успешный GitHub Actions run `32945172475` со статусом `Send report: success`.

- `85ffa52` — `Добавить временную диагностику чисел`.
  До 18 сентября 2026 года включительно внутренняя beta-версия сохраняет в отдельную временную базу каждый встретившийся числовой случай: полное число, локальный текст до и после обработки, статус, вид конструкции, конкретное основание решения, коды сработавших правил, версию числовых правил, дату сборки и случайный номер запуска. Полные числа передаются по прямому решению автора; имена пользователей, названия и ссылки на файлы, страницы и слои, внутренние номера слоёв и координаты в базу не попадают. Диагностические тексты не отправляются в PostHog или Telegram.
  Добавлен отдельный надёжный путь данных: Figma хранит неотправленные пакеты в ограниченной локальной очереди и повторяет попытку при следующем запуске, Vercel строго проверяет разрешённые поля, Neon Postgres хранит строки, а после итогового отчёта за 18 сентября подробные данные автоматически удаляются. Сбор прекращается в полночь 19 сентября по Москве. Ежедневный Telegram-отчёт получает только количество случаев за день и ссылку на закрытую страницу.
  Создана закрытая страница `/number-diagnostics` с входом по паролю, нажимаемыми сводными счётчиками, периодом, поиском, фильтрами по причине, правилу и числу слоёв, сравнением текста до и после, постраничной загрузкой и выгрузкой выбранных или всех случаев в UTF-8 CSV. Реальные символы пробелов в выгрузке не нормализуются. На странице неразрывные пробелы по умолчанию обозначаются бордовой звёздочкой без отдельной сложной легенды. Вкладка браузера использует присланный beta-логотип `public/favicon.png`.
  Причины решений сделаны конкретными: положение валюты или единицы, защита номера счёта, карты, маски, телефона, даты, кода и отсутствие признака количества. Для количества соседний слой показывается только тогда, когда основной алгоритм действительно использовал его как доказательство. Для защищённых счетов и кодов диагностика дополнительно находит поясняющую подпись в той же визуальной строке даже через декоративную линию и вложенные контейнеры, поэтому в отчёте видны конструкции `Счёт зачисления · 123…` и `ID · A417…`. Этот координатный поиск работает только для понятности отчёта и не даёт типографу новых оснований менять текст.
  Стартовый слепок числового алгоритма помечен как `numbers-2026-08-25-v1`; диагностическая схема поднята до версии `2`, чтобы новые карточки с исправленным контекстом отличались от старых без ложного изменения версии правил. README, описание аналитики, план числовой политики и новый `docs/number-diagnostics.md` подробно фиксируют срок, состав данных, приватность, страницу, выгрузку, версии, историю сборок, удаление и границы влияния.
  Затронуты `src/code.ts`, сгенерированный `dist/code.js`, `manifest.json`, README, документы, ежедневный отчёт, новые серверные точки `api/number-diagnostics*`, библиотека `scripts/lib/number-diagnostics-*`, закрытая страница в `public`, зависимости Neon/Vercel и автоматические тесты. Типографические правила, вкладка `Правила`, уведомления и остальные видимые тексты Figma-плагина не менялись.
  Проверки: `npm test`, TypeScript-сборка, тесты типографики, аналитики и временной диагностики, `git diff --check`, проверка закрытой страницы и API после публикации на Vercel, сохранение настоящих пробелов в CSV, приём второй версии схемы, точное совпадение опубликованной фавиконки с присланным PNG и тест координатного поиска на 4000 слоях. Перед публикацией основной beta-версии в Figma нужно выполнить `Publish new version`; после публикации новые карточки должны показывать `Сборка диагностики: 2` и контекст подписей при запуске по всему экрану или общему контейнеру.

- `3f5bc86` — `Не менять длинные цепочки точек`.
  Исправлена ошибка правила многоточия: раньше код заменял любые первые три точки внутри более длинной непрерывной цепочки, поэтому пять точек превращались в знак многоточия и две обычные точки, а длинные точечные разделители в макетах разрушались. Теперь замена выполняется только для отдельной последовательности ровно из трёх точек, рядом с которой слева и справа нет ещё одной точки.
  Результат соответствует уже согласованной строке вкладки `Правила`: `...` превращается в `…`, а `....`, `.....`, `......` и более длинные цепочки остаются без изменений. Видимый текст вкладки `Правила` и краткая формулировка README не менялись по прямой договорённости с пользователем. Подробный внутренний справочник уточнён, потому что раньше ошибочно описывал замену «первых трёх» точек в любой цепочке.
  Добавлены автоматические проверки отдельного многоточия, трёх точек внутри слова, цепочек из четырёх, пяти и шести точек и устойчивости повторного запуска. Сборка `dist/code.js` обновлена.
  Затронуты `src/code.ts`, сгенерированный `dist/code.js`, `scripts/test-clean-typography.js` и `docs/typography-rules.md`. Остальные правила типографики, порядок их выполнения, UI, уведомления, аналитика, Telegram/Vercel и `manifest.json` не менялись.
  Проверки: `npm test`, TypeScript-сборка, все тесты типографики, точечной записи и аналитики, `git diff --check`, ручная проверка пользователем в настоящем макете Figma с длинными точечными разделителями. Пользователь подтвердил, что после исправления результат правильный.
- `e990ad9` — `Добавить рабочий экран проблемных слоёв`.
  Согласованный экран ошибок перенесён из предпросмотра в настоящий плагин. Если отдельный текстовый слой не удалось безопасно изменить, но его исходное состояние подтверждённо восстановлено, Чистовик сохраняет этот слой без изменений, продолжает обработку остальных и в конце показывает список пропущенных слоёв. Если восстановление текста, оформления или компонентной связи подтвердить не удалось, обработка останавливается, опасный слой выделяется отдельно, а все последующие слои помечаются как необработанные. Ошибка до начала обработки показывает отдельное состояние без списка слоёв.
  Экран одинаково работает в быстром запуске и обоих режимах настроек. Первый проблемный слой автоматически выделяется и показывается в макете; по списку можно переходить кликом и стрелками. Кнопка `Вернуться к типографу` появляется только после запуска из настроек. Сохранены согласованные размеры окна, тексты, склонение счётчиков, двухстрочное сокращение длинного текста, постоянная полоса прокрутки, выбранное состояние слоя и встроенные иллюстрации. Изображения и шрифт значка `Т` встраиваются в сборку, поэтому окно не зависит от доступа к внешним сайтам.
  Аналитика дополнена только числовыми счётчиками: успешно обработанные слои, безопасно пропущенные слои, критические ошибки восстановления и слои, до которых обработка не дошла. Текст, название, путь и идентификатор слоя в PostHog не передаются. Схема аналитики поднята до версии `13`, а серверный приём обновлён тем же разрешённым списком полей.
  Автоматические тесты проверяют продолжение после безопасной ошибки, остановку только при неподтверждённом восстановлении, состав списков и счётчиков, выбор первого слоя, отсутствие прежнего красного финального уведомления, все согласованные тексты и встроенные материалы интерфейса, а также приватность аналитики. README, план развития, описание ошибок и аналитики, дизайн-система и ручная памятка Figma приведены в соответствие с фактическим поведением.
  Затронуты `src/code.ts`, `src/ui.html`, сгенерированный `dist/code.js`, `scripts/sync-ui-content.js`, тесты типографики и аналитики, серверный разрешённый список аналитики, README и документы в `docs`. Типографические правила и их порядок не менялись.
  Проверки: `npm test`, TypeScript-сборка, тесты типографики, точечной записи, интерфейса и аналитики, `git diff --check`, ручная проверка всех трёх искусственно вызванных состояний в Figma и последующая проверка настоящей обработки после полного удаления временного тестового режима. Пользователь подтвердил работу стрелок, кликов, выделения слоя, кнопки возврата и обычной успешной обработки.
- `7d0401d` — `Ускорить точечную обработку и усилить защиту Figma`.
  После серии ручных проверок и расследования зависаний переработаны самые тяжёлые части точечной обработки. Для длинных текстов, где сумма исходной и готовой версии превышает 4096 видимых символов, сначала используется ограниченный поиск ближайшего устойчивого совпадения в пределах 32 символов. Поиск идёт от меньшего расстояния к большему и останавливается на первом правильном кандидате; если безопасное локальное совпадение не найдено, сохраняется прежний точный расчёт. Готовая строка и карта оформления теперь собираются последовательным проходом вместо многократного разрезания растущих строк и массивов.
  Плотные исправления больше не отклоняются по прежним ограничениям общей длины и числа операций. Если в слое больше 256 правок, близкие операции объединяются только внутри одного участка с одинаковым оформлением. В однородном слое такой участок может быть большим, но плагин по-прежнему использует `deleteCharacters` и `insertCharacters`, не присваивает новую строку через `textNode.characters`, не пересоздаёт текстовый слой и не отвязывает компонент. Добавлен предельный тест из 10 000 сочетаний `слово...`: все они превращаются в знаки многоточия, а Figma получает одну объединённую запись для однородного участка вместо 10 000 отдельных команд.
  Ускорены остальные места, которые особенно заметны на больших фреймах. Определение русского или английского оформления кавычек вычисляется один раз для каждой строки, разбиение длинных чисел на разряды строится последовательными группами, математический разбор пропускает уже прочитанное обычное число целиком, а поиск телефонного продолжения для отдельного слоя `+7` группирует и сортирует кандидатов внутри одного контейнера и проверяет только ближайшие справа. Чтение пустых идентификаторов оформления в длинном слое без библиотечного стиля выполняется один раз для участка, а не для каждого символа.
  Усилена защита Figma. Контрольная точка для общей отмены создаётся непосредственно перед первым реальным изменением, а не в начале подготовки; поэтому ручная правка другого слоя во время загрузки шрифтов не должна исчезнуть при аварийной отмене Чистовика. Снимок каждого изменяемого слоя дополнен привязкой текста к свойству компонента, полной цепочкой родителей и идентификаторами главных компонентов всех родительских экземпляров. Эти данные проверяются после успешной записи и повторно после отмены. Пустой ответ Figma для идентификатора стиля отдельного диапазона больше не стирает уже известную связь: используется идентификатор сохранённого участка или всего слоя.
  Ожидание загрузки страницы, шрифта и главного компонента ограничено 15 секундами; в сохранённом старом режиме полной записи такое же ограничение действует для библиотечного стиля и переменной. Зависшее обращение теперь получает категорию `timeout` вместо бесконечного состояния работы. Ошибки `Style does not exist` и `Variable does not exist` больше не маскируются под удалённый слой. Точечный режим намеренно не открывает и не переподключает библиотечные стили и переменные перед записью: он оставляет существующие связи на месте и проверяет их идентификаторы и свойства после изменения. Старый режим полной записи продолжает отдельно проверять доступность ресурсов.
  Возвращено дословно согласованное поведение пунктуации: только отдельная пара `!?`, рядом с которой нет других вопросительных или восклицательных знаков, меняется на `?!`. Более длинные смешанные последовательности `?!?!`, `?!?` и `!?!!` больше не сокращаются целиком. После переноса `?` или `!` внутрь кавычек повторные знаки и отдельная пара `!?` проверяются ещё раз в том же запуске. Поэтому `«Ты правда спросил „зачем?“?»` сразу превращается в `«Ты правда спросил „зачем?“»`, а не требует второго запуска. Новых пунктов на вкладке `Правила` и новых видимых текстов не добавлялось.
  Автоматические тесты расширены 20 000 случайных сравнений ускоренного поиска с прежним полным перебором, сравнением новой последовательной карты оформления с прежним способом, повторяющимися строками, длинным однородным и смешанным текстом, 10 000 многоточий, таймаутом Figma, отсутствующим стилем и переменной в старом режиме, сохранением вложенных компонентных связей, текстового свойства компонента, правильным моментом контрольной точки, аварийной отменой и однопроходной пунктуацией в обычном режиме и режиме `Для разработки`.
  README, план развития, описание аналитики и диагностики ошибок, справочник правил и ручная памятка Figma приведены в соответствие с фактическим кодом. Удалено старое неверное утверждение, что точечный режим перед записью заново открывает связанные стили и переменные. Зафиксирована ручная проверка 6 августа 2026 года на фрейме `975:2363`: 57 доступных текстовых слоёв, 56 участков с библиотечными текстовыми стилями, 4 участка со связанными цветами и 409 экземпляров компонентов сохранились без потерянных связей; во вложенной пунктуации не осталось повторных знаков. Отдельно записано, что заблокированные блоки `Как должно быть` в этом тестовом фрейме устарели: буквально актуальны только 6 из 22 разделов, поэтому их нужно синхронизировать со справочником до следующей полной проверки.
  Затронуты `src/code.ts`, сгенерированный `dist/code.js`, `scripts/test-clean-typography.js`, README, `docs/development-roadmap.md`, `docs/error-monitoring.md`, `docs/figma-regression-checklist.md`, `docs/plugin-analytics.md` и `docs/typography-rules.md`. Рабочий UI, согласованные уведомления, `manifest.json`, транспорт аналитики, Telegram-отчёты и схема событий не менялись.
  Проверки: `npm test`, TypeScript-сборка, все тесты типографики, точечной записи, сохранения оформления, аналитики и Telegram-отчётов, `git diff --check`, программная проверка реального Figma-фрейма и отдельных скриншотов вложенной пунктуации и библиотечного компонента.
- `e5ac107` — `Защитить точечную запись и аналитику`.
  Проведён расширенный аудит безопасной точечной записи. Улучшено сохранение смешанного оформления, дополнительных свойств шрифта, библиотечных связей, свойств компонентов, положения слоя в документе и служебных данных режима разработки. Большие наборы точечных операций объединяются без изменения результата, а запасное деление текста не разрывает эмодзи, индийские сочетания и корейские слоги. Неудачная загрузка шрифта больше не блокирует повторную попытку.
  Добавлена защита от изменений во время ожидания Figma. Если изменился текст, плагин ничего не записывает и показывает согласованное уведомление `Тут изменился текст — запустите типограф заново 🔄`. Если изменилось только оформление, плагин перечитывает его, при необходимости загружает новый шрифт и повторно проверяет состояние не больше трёх раз. Удалённый до записи слой безопасно пропускается, но отсутствие шрифта, стиля или переменной не считается удалением. Критическая ошибка неподтверждённого возврата остаётся важнее остальных сообщений.
  Уточнены уже существующие правила: смешанные серии вопросительных и восклицательных знаков стабильно приводятся к `?!`, а диапазоны повторно проверяются после оформления разрядов в числах. Новых правил не добавлялось; вкладка `Правила` не менялась по отдельной договорённости с пользователем.
  Приём аналитики на Vercel ограничен белым списком событий и полей: проверяются виды и безопасный формат значений, а посторонние поля и пользовательский текст отклоняются. GitHub Actions проверяет Node.js 20 и 22 и контролирует, что сборка не оставляет незаписанных файлов. В `AGENTS.md` закреплено главное правило общения с пользователем простым разговорным языком на русском. Обновлены README, план развития, описание аналитики и ошибок, ручная проверка Figma и автоматические тесты; пересобран `dist/code.js`.
  Проверки: многократный `npm test`, TypeScript-сборка, тесты типографики и аналитики, `git diff --check`. Рабочий интерфейс, `manifest.json` и вкладка `Правила` не менялись.
- `62b760d` — `Добавить надёжную очередь аналитики`.
  Исправлен путь доставки аналитики для пользователей, у которых прямой адрес PostHog блокируется: Figma-плагин теперь отправляет события только на `https://chistovik-plugin.vercel.app/api/capture`. Vercel проверяет событие и подтверждает приём только после надёжного сохранения в теме Vercel Queues `chistovik-analytics`. Закрытый обработчик `api/queues/posthog.js` доставляет событие в PostHog и при сбое автоматически повторяет попытки; временное хранение ограничено семью днями. Одинаковый `uuid` используется как ключ защиты от дублей в очереди и в PostHog.
  В PostHog добавляются безопасные поля `analytics_transport = vercel_queue_v1`, `analytics_delivery_delay_ms`, `analytics_queue_wait_ms` и `analytics_queue_delivery_count`. Telegram-бот по-прежнему считает только уже доставленные в PostHog события; временную очередь он не смешивает с итоговой статистикой, а ранее отправленная ежедневная сводка задним числом не исправляется.
  Разрешённый сетевой домен в `manifest.json` заменён с PostHog на Vercel, публичный токен записи PostHog удалён из Figma-сборки, схема аналитики поднята до `9`, маркер сборки и отсчёт исходной статистики перенесены на `2026-08-05`. Если публикация в Figma произойдёт позже, дату сборки и начало семидневного окна нужно перед публикацией синхронно изменить. Добавлена зависимость `@vercel/queue`, отдельные проверки приёма, временного хранения, защиты от дублей, доставки и ошибок очереди. Типографические правила, тексты UI и уведомлений, обработка текста, стили и связи с компонентами не менялись. Проверки: `npm test`, TypeScript-сборка, JavaScript-проверки Vercel-функций, загрузка обработчика очереди, `npm audit --omit=dev` без найденных уязвимостей и `git diff --check`.
- `58d2522` — `Добавить статус готовности к точечной обработке`.
  В ежедневный Telegram-отчёт и команду `/today` после заголовка добавлен временный статический абзац без счётчиков. Пока не выполнены одновременно семь полных дней исходного замера и 30 успешных обработок сборки `2026-07-31` с версией измерений `3`, показывается согласованный красный текст. После выполнения обоих условий он автоматически заменяется согласованным зелёным текстом.
  Для начала точечных доработок добавлен отдельный этап `implementation`: при его включении временный абзац полностью исчезает. Обязательное переключение без напоминания пользователя сохранено в `AGENTS.md` и плане развития. Добавлены проверки красного, зелёного и удалённого состояний, отсутствия счётчиков, сообщения в день без запусков и правильного отсчёта полных дней. Код Figma-плагина, типографические правила, обработка текста, стили и связи с библиотекой не менялись. Для применения достаточно обновления Vercel; публиковать новую версию плагина в Figma не нужно. Проверки: `npm test`, JavaScript Vercel-endpoints и `git diff --check`.
- `6482cf4` — `Обновить данные предпросмотра ошибок`.
  В тестовых данных согласованного окна ошибок обычное состояние теперь показывает три пропущенных слоя, а критическое — один слой для проверки стилей и три необработанных слоя. Все общие и групповые счётчики вычисляются из фактического числа строк: `3 слоя пропущены · 10 успешно`, `4 слоя нужно проверить · 10 успешно`, `Не удалось обработать · 3`.
  Иллюстрация состояния `Не удалось запустить типограф` заменена на новую версию с прозрачным фоном. Документы `docs/error-ui-plan.md` и `docs/ui-design-system.md` синхронизированы с предпросмотром. Рабочий UI плагина, типографические правила, уведомления и обработка текста не менялись. Проверки: JavaScript предпросмотра, визуальная проверка третьего состояния, `npm test`, TypeScript-сборка и `git diff --check`.
- `c4e417d` — `Доработать дизайн окна ошибок`.
  Финально уточнён интерактивный предпросмотр будущего окна проблемных слоёв. В обычном состоянии согласованы тексты `Эти слои нужно исправить вручную` и динамический счётчик вида `4 слоя пропущены · 10 успешно`; в критическом — подзаголовок `У одного слоя могли сломаться стили` и короткие числа в заголовках групп без повторения слова `слой`. Склонение счётчика учитывает формы `1 слой пропущен`, `2 слоя пропущены`, `5 слоёв пропущены` и `21 слой пропущен`.
  Выбранный текстовый слой получил вертикальную версию фирменной линии активной вкладки: использованы те же цвет `#065C5B`, толщина, асимметричные скругления, тень и наклон. Линия занимает всю высоту строки, плотно прилегает к левому краю, а светлая заливка не просвечивает под её окончаниями. Заменены одинаковые по размеру иконки обычной и критической ошибки и иллюстрация состояния, когда типограф не запустился. `docs/error-ui-plan.md` и `docs/ui-design-system.md` синхронизированы с макетом.
  Это по-прежнему только согласованный дизайн: рабочий UI, типографические правила, уведомления Figma, аналитика и обработка текста не менялись. Проверки: JavaScript предпросмотра, визуальная проверка всех трёх состояний, `npm test`, TypeScript-сборка и `git diff --check`.
- `c7534ee` — `Зафиксировать дизайн окна ошибок`.
  Полностью согласован будущий экран проблемных слоёв и обновлён интерактивный предпросмотр `preview/error-report-preview.html`. Окно теперь совпадает по размеру с настройками и показывает три состояния: обычные проблемные слои, критическую остановку при неподтверждённом восстановлении оформления и невозможность запустить типограф до начала обработки. Зафиксированы точные тексты, автоматические счётчики и склонения, группировка критических слоёв, автоматический выбор первого слоя, навигация стрелками, переход к слою, двухстрочные фрагменты текста, постоянная полоса прокрутки и кнопка только после запуска из настроек.
  Добавлены согласованные иллюстрации состояний. Значок текстового слоя `Т` использует локальный поднабор официального шрифта Cactus Classical Serif и больше не зависит от Google Fonts; лицензия хранится рядом. Полностью переписаны устаревшие документы про две колонки и технические подсказки: `docs/error-ui-plan.md` теперь является полным планом реализации, а `docs/ui-design-system.md` — точным визуальным источником. README, план развития и граница между локальным окном и аналитикой также актуализированы.
  Это пока только готовый дизайн и документация: окно ещё не перенесено в `src/ui.html`, рабочая логика плагина и текущие уведомления Figma не менялись. Проверки: JavaScript предпросмотра, визуальная проверка всех трёх состояний, склонения счётчиков, `npm test`, TypeScript-сборка и `git diff --check`.
- `ae23ad7` — `Защитить аналитику от дублей`.
  Найдена причина невозможной статистики за 30 июля: один запуск плагина отправил два одинаковых события об успехе, поэтому отчет показывал 17 успешных обработок при 16 запусках. Каждое событие аналитики теперь получает настоящий постоянный UUID, который не меняется при повторной отправке из очереди; PostHog распознает повтор как то же событие и не сохраняет его второй раз. Старые записи очереди автоматически получают совместимый UUID.
  Telegram-отчеты считают запуски, успехи, ошибки, режимы, области, опции, причины ошибок и показатели скорости по уникальному номеру запуска, а для старых событий без такого номера используют UUID записи PostHog. Поэтому повторно отправленные события не увеличивают статистику и не искажают среднее время. Схема аналитики поднята до `7`; типографические правила, результат обработки текста, оформление, библиотечные связи, UI и уведомления не менялись.
  Затронуты `src/code.ts`, сгенерированный `dist/code.js`, `scripts/lib/analytics-report.js`, тесты типографики и Telegram-аналитики, а также `docs/plugin-analytics.md`. Действующие графики PostHog переведены на тот же подсчет уникальных запусков; значение за 30 июля подтверждено как 16 запусков, 16 успехов и 0 ошибок. Двойная тестовая отправка одного UUID сохранилась в PostHog один раз. Проверки: `npm test`, TypeScript-сборка и `git diff --check`.
- `435a1a5` — `Обновить макет окна ошибок`.
  Пересобран статический интерактивный предпросмотр будущего окна ошибок. В левой панели показаны проблемные текстовые слои, выбор строки сразу имитирует переход к слою в макете, а справа остается короткая подсказка, что именно проверить. Убраны технические коды и отдельная кнопка перехода к слою; добавлены сценарии частичной обработки, возможной проблемы оформления и невозможности обработать слой.
  Изменен только `preview/error-report-preview.html`: это проектный макет, он пока не подключен к Figma-плагину и не меняет текущие уведомления. Тексты будущего интерфейса остаются черновыми и должны быть дословно согласованы перед реализацией.
- `bf29f1a` — `Исправить подсчёт сработавших правил`.
  Исправлена найденная при проверке PostHog ошибка аналитики: внутреннее временное превращение существующих неразрывных пробелов в обычные и обратно больше не считается реальным исправлением. Изменения правил и их последовательные пары сначала накапливаются отдельно для каждого слоя и подтверждаются только после сравнения окончательного текста в Figma с исходным. Если текст не изменился или был успешно возвращён после ошибки оформления, временные срабатывания отбрасываются; число проверок и время правил сохраняются.
  Схема аналитики поднята до `6`, версия аналитики правил — до `2`, дата сборки — `2026-07-31`. Добавлены тесты уже чистого текста с неразрывными пробелами, действительно изменённого текста и успешного возврата исходного состояния после ошибки. Типографические правила, оформление, UI, уведомления, настройки и manifest не менялись.
  Проверки: `npm test`, TypeScript-сборка, `git diff --check` и ручной запуск в Figma. В PostHog подтверждено: 25 обработанных слоёв, 3424 символа, 0 реальных изменений, 0 сработавших правил, 0 пар правил, все 77 кодов в сводке, 0 ошибок и 48 мс общего времени.
- `922ca50` — `Добавить аналитику типографических правил`.
  Создан единый справочник `docs/typography-rules.md`: 77 действующих правил получили постоянные внутренние коды, согласованные тексты вкладки `Правила`, полное описание фактической логики, примеры, защиты и место в порядке обработки. В `docs/development-roadmap.md` сохранен подробный план перехода к точечным изменениям и последующего аудита правил.
  Итоговые события PostHog теперь содержат агрегированную статистику по всем кодам без отдельных сетевых запросов: число проверок и применений, измененные слои, время, самое медленное и самое активное правило, код правила при ошибке и последовательные пары правил. Схема аналитики поднята до `5`, версия измерений производительности — до `3`, версия аналитики правил — `1`, дата сборки — `2026-07-30`. Telegram-отчеты о скорости переведены на новую сопоставимую версию измерений.
  Типографические правила, их порядок и результат обработки не менялись; видимые тексты, уведомления, UI, настройки и manifest также не менялись. Затронуты `src/code.ts`, сгенерированный `dist/code.js`, тесты типографики и Telegram-аналитики, `scripts/lib/analytics-report.js`, README и документация. Проверки: `npm test`, TypeScript-сборка, уникальность и полнота 77 кодов, 40 000 сравнений результата типографики с предыдущей версией, `git diff --check` и два ручных запуска в Figma. В PostHog оба запуска завершились успешно, передали все 77 кодов и не зафиксировали ошибок или неудачного возврата оформления.
- `57ba8b6` — `Объединить документацию аналитики плагина`.
  Разрозненные документы про PostHog и Telegram объединены в единый файл `docs/plugin-analytics.md` с понятным автору и технически точным описанием всего пути данных: события из Figma, анонимные идентификаторы, очередь отправки, приватность, два дашборда PostHog, ежедневная сводка, команды `/today`, `/speed` и `/errors`, правила расчёта показателей, Vercel, webhook, секреты и диагностика сбоев. Будущие Telegram-команды удалены, а идея платного аккаунта и будущей идентификации перенесена в `docs/future-ideas.md`.
  README сокращён до обзора и ссылки на единый источник правды. Удалены старые `docs/analytics-plan.md`, `docs/telegram-bot-guide.md`, общий HTML-предпросмотр дизайн-системы и старый `preview/ui-preview.html`. Материалы будущего окна ошибок сохранены; в документации отдельно отмечено, что перед реализацией сценарий нужно повторно согласовать с одинаковыми уведомлениями Figma во всех режимах.
  Код плагина, собранный `dist/code.js`, правила типографики, уведомления, PostHog, Telegram/Vercel и manifest не менялись. Проверки: сверка описанных команд с `api/telegram.js` и `scripts/configure-telegram-menu.js`, проверка актуальных ссылок и `git diff --check`.
- `10bcf0a` — `Дополнить правила дат и единиц`.
  Числовые даты с запятыми приводятся к записи с точками: `12,05,2025` -> `12.05.2025`. Исправляются только возможные календарные даты; невозможные даты, смешанные разделители, версии, коды и более длинные числовые последовательности защищены от соседних правил. По прямому решению автора отдельная строка об этом не добавлялась во вкладку `Правила`: поведение считается уточнением уже описанного правила о датах.
  Добавлены технические единицы с согласованным регистром и неразрывным пробелом: `В`, `Вт`, `кВт`, `dpi`, `lpi`. Слитная запись и другой регистр нормализуются: `220в` -> `220 В`, `100ВТ` -> `100 Вт`, `2КВТ` -> `2 кВт`, `300DPI` -> `300 dpi`, `150LPI` -> `150 lpi`. Дата во фразе `10.04 В Москве` не превращается в десятичное число. Одиночный знак градуса теперь пишется слитно с числом: `20 °` -> `20°`, а температура сохраняет неразрывный пробел: `20 °C`, `100 °F`.
  Вкладка `Правила` обновлена дословно согласованными строками в разделах `Сокращения`, `Неразрывные пробелы` и `Математика и символы`; README синхронизирован. Добавлен `docs/future-ideas.md` с идеями ёфикатора, расширенных неразрывных пробелов, справочника телефонных кодов, постоянных кодов правил и результатами конкурентного аудита.
  Затронуты `src/code.ts`, сгенерированный `dist/code.js`, `scripts/test-clean-typography.js`, `src/ui-content.js`, сгенерированный `src/ui.html`, README и `docs/future-ideas.md`. Логика сохранения оформления, библиотечных связей, обхода слоёв, аналитики и уведомлений не менялась.
  Проверки: `npm test`, TypeScript-сборка, 42 новых защитных примера для дат, единиц и градусов, прежние тесты типографики и аналитики, `git diff --check`. Перед публикацией в Figma полезно вручную проверить один слой со смешанным оформлением на примерах `12,05,2025`, `220 В`, `2 кВт`, `300 dpi`, `20 °` и `20 C`.
- `39cc90e` — `Ускорить типограф и исправить ошибки обработки`.
  Все способы запуска теперь используют одинаковые уведомления Figma: постоянное сообщение о работе гарантированно сменяется одним финальным результатом, повторный сигнал присоединяется к текущей обработке, а индикатор в кнопке настроек остается дополнительным. Критическое предупреждение о стилях показывается только тогда, когда после ошибки не удалось восстановить и повторно подтвердить исходный текст, внешний вид и библиотечные связи; успешный возврат получает обычную ошибку обработки.
  Устранена найденная по PostHog задержка около 80 секунд на двух коротких слоях: для единообразно оформленного слоя плагин не привязывает повторно уже сохраненный библиотечный text/fill style. После записи проверяются точные style id и все сохраненные свойства оформления; при отличии выполняется проверяемый возврат. Смешанное оформление продолжает использовать прежний осторожный путь. Плагин по-прежнему не отвязывает инстансы, не создает новые текстовые слои и не переходит к точечной записи.
  Правило чисел различает дроби и нумерацию. Точки сохраняются и восстанавливаются в заголовках и документах: `3,1 Как использовать` -> `3.1 Как использовать`, `пункт 5,5` -> `пункт 5.5`, но `4,5 кг`, деньги и проценты остаются дробями. Вкладка `Правила` обновлена согласованными строками про десятичную запятую и нумерацию. Создан `AGENTS.md`, который запрещает менять типографику до дословного согласования соответствующего текста вкладки и требует синхронно обновлять README.
  Ежедневный Telegram-отчет больше не показывает пустой блок производительности без чисел. Затронуты `src/code.ts`, сгенерированный `dist/code.js`, тесты типографики и Telegram-аналитики, `scripts/lib/analytics-report.js`, `src/ui-content.js`, сгенерированный `src/ui.html`, README, UI-дизайн-система и `AGENTS.md`.
  Проверки: ручные запуски в Figma в быстром режиме и режиме `Для разработки` на двух коротких слоях с библиотечным стилем, `npm test`, TypeScript-сборка, тесты сохранения и аварийного возврата оформления, защитные тесты соседних числовых правил, Telegram-тесты и `git diff --check`.
- `e18f558` — `Подготовить точные замеры производительности`.
  Перед исходным семидневным замером выполнен безопасный пакет без перехода к точечной записи. Этапы обработки и самый медленный слой теперь измеряются монотонным таймером с долями миллисекунды. Аналитика получила схему `4`, дату сборки `2026-07-29`, `performance_measurement_version: 2` и счётчики попыток аварийного возврата оформления. Ежедневный и недельный Telegram-отчёты строят выводы о скорости только по событиям с новой полной диагностикой; старые события продолжают участвовать в общих запусках и ошибках.
  Ускорена текущая полная замена строки: для слоя с одним оформлением не строится тяжёлая карта символов; одинаковые строки при возврате сопоставляются напрямую; один текстовый слой не запускает межслойный поиск телефона; одинаковая неудачная загрузка шрифта не повторяется до следующего запуска; состояния общих скрытых/заблокированных родителей, переменные Figma и неизменившиеся служебные данные переиспользуются.
  Усилена безопасность оформления: текст с bound variables не использует сокращённое восстановление, а переменные возвращаются после подключённого text style. Если обычное восстановление падает, исходный текст и оформление возвращаются с отдельным замером. Если аварийный возврат тоже падает, обработка остальных слоёв немедленно останавливается, `rollback_failed` получает приоритет над ранними ошибками, а пользователь видит `Не удалось вернуть оформление. Проверьте текстовый слой 🛑`.
  Тесты расширены дробными таймерами, быстрым планом единого оформления, длинным текстом, кешами, отсутствием пустых plugin data-записей, style id с переменной, полной mixed-style обработкой, успешным и двойным провалом возврата, а также сценарием `ошибка шрифта → rollback_failed`. Три независимых повторных ревью подтвердили устранение найденных блокеров.
  Затронуты `src/code.ts`, сгенерированный `dist/code.js`, тесты типографики и Telegram-аналитики, `scripts/lib/analytics-report.js`, README, `docs/analytics-plan.md` и `docs/figma-regression-checklist.md`. Точечная запись, порядок типографических правил, UI настроек и manifest не менялись.
  Проверки: `npm test`, TypeScript-сборка, тесты типографики/оформления/аналитики/Telegram, `git diff --check`, три независимых ревью. Перед публикацией обязательно пройти `docs/figma-regression-checklist.md` на настоящем библиотечном компоненте. Дата `2026-07-29` верна только при публикации в этот день; при публикации позже её нужно обновить перед сборкой.
- `1eb4cd3` — `Добавить двойную проверку плагина`.
  Добавлен GitHub Actions workflow `Автоматические тесты`: после каждого push и pull request GitHub выполняет `npm ci` и `npm test`. В README добавлены статус проверки и описание двойного контроля — локально перед коммитом и повторно в GitHub.
  Автоматические тесты расширены проверками сохранения библиотечных text styles, ручной жирности и курсива поверх них, списков и абзацных отступов, повторяющихся фрагментов с разным оформлением, неизмененного текста, банковских номеров, разнесенных по слоям телефонов, ошибок загрузки шрифта и возврата исходного текста после ошибки восстановления оформления. Дополнительно зафиксирован запрет на отвязку инстансов и создание нового текстового слоя вместо исходного.
  По найденным тестами случаям добавлены защитные исправления: ручная жирность и курсив возвращаются после библиотечного стиля; списки не используют сокращенное восстановление; при ошибке возврата оформления плагин пытается вернуть исходный текст; телефонные части сопоставляются только внутри одного контейнера; защищены сгруппированные карты и счета; сохраняется точка у инициала в `Иванов А./Петров Б.`.
  Добавлен `docs/figma-regression-checklist.md` для проверки в настоящей Figma тех свойств библиотечных компонентов, которые нельзя полностью воспроизвести имитацией API.
  Затронуты `src/code.ts`, сгенерированный `dist/code.js`, `scripts/test-clean-typography.js`, `.github/workflows/tests.yml`, README и `docs/figma-regression-checklist.md`. UI, уведомления, настройки, manifest, PostHog и Telegram/Vercel не менялись.
  Проверки: `npm test`, TypeScript-сборка, `git diff --check`. После push нужно дождаться зеленого результата workflow `Автоматические тесты / Сборка и тесты`, затем вручную выполнить короткую проверку реального библиотечного компонента по памятке.
  Первый удаленный запуск GitHub успешно завершился за 10 секунд. После него служебные шаги `actions/checkout` и `actions/setup-node` обновлены с v4 до актуальной v6, чтобы убрать предупреждение GitHub об устаревшем Node.js 20 внутри самих Actions.
- `11cd966` — `Разрешить push после коммита`.
  Правила разработки в README обновлены по явному решению пользователя: запрос `сделай коммит` теперь означает, что после успешных проверок агент создает коммит и самостоятельно выполняет `git push` текущей ветки в `origin`. Оставлять коммит только локально нужно лишь тогда, когда пользователь прямо об этом попросил. Прежние указания про ручной push через GitHub Desktop удалены.
  Затронут только README; код Figma-плагина, UI, типографические правила, уведомления, тесты, сборка, аналитика и Telegram/Vercel не менялись.
  Проверка: `git diff --check`.
- `f76cd00` — `Улучшить запуск и обработку типографа`.
  При любом запуске типографа появляется постоянное уведомление `Чистовик работает...`, которое снимается перед существующим финальным уведомлением успеха или ошибки. Красный тост `Ой, не получилось почистить 🛑` теперь показывается `4000 ms`, как успешные уведомления. Запуск из окна продвинутых фич дополнительно показывает CSS-спиннер в кнопке, блокирует повторный клик и гарантированно возвращает кнопку в обычное состояние после успеха или ошибки. В ядре добавлена защита от параллельных повторных запусков; кнопка остановки не добавлялась.
  Одинаковые шрифты теперь загружаются через `figma.loadFontAsync()` один раз за запуск: остальные текстовые слои переиспользуют Promise по паре `family/style`, а отклоненный Promise удаляется из кеша и допускает повторную попытку. Это не меняет набор шрифтов, порядок обработки текста и восстановление стилей.
  Исправлена ложная точка внутри slash-сокращений: конструкции `ж/д`, `д/к`, `п/п`, `а/д`, `руб/мес` и `кв/м` воспринимаются целиком, а `руб/кв. м` сохраняет точку после `кв.`. Добавлены регрессионные тесты этих сокращений, полного исходного примера и кеша шрифтов, включая mixed-font слой и повторную попытку после ошибки загрузки.
  Затронуты `src/code.ts`, сгенерированный `dist/code.js`, `src/ui.html`, `scripts/sync-ui-content.js`, `scripts/test-clean-typography.js` и README. Правила типографики вне slash-сокращений, порядок правил, mixed styles, development-маркеры, настройки запуска, тексты вкладки `Правила`, `manifest.json`, аналитика и Telegram/Vercel не менялись.
  Проверки: `npm test`, TypeScript-сборка, `git diff --check`, визуальная проверка обычного и loading-состояния кнопки в окне `360×372`. В Figma после обновления нужно проверить быстрый запуск, оба режима из окна продвинутых фич, замену `Чистовик работает...` на успех и ошибку, блокировку повторного клика, а также `ж/д билеты` и `100 руб/кв. м`.
- `250216d` — `Добавить диагностику отчета PostHog`.
  Telegram-отчет больше не превращает неожиданный формат ответа PostHog в отчет с ложными нулями. `fetchPostHogSummary` теперь проверяет, что PostHog вернул строку результата с ожидаемым количеством колонок; при пустом или нестандартном `results` формируется диагностическое сообщение: жирный заголовок `🛑 Не удалось собрать отчёт за <дата>`, причина `PostHog вернул неожиданный формат данных.` и ссылка `в полном дашборде`.
  Ежедневный Vercel Cron, команда `/today` и резервный скрипт GitHub Actions переведены на общий безопасный путь `createAnalyticsMessageOrDiagnostic`. Реальные нули при корректном ответе PostHog по-прежнему остаются обычным отчетом.
  README и `docs/telegram-bot-guide.md` обновлены описанием диагностического сообщения. Добавлен тест `scripts/test-analytics-report.js`, который проверяет диагностический текст, отсутствие строки `Запуски типографа: 0` при нестандартном ответе и сохранение обычного нулевого отчета при корректной строке результатов.
  Затронуты только Telegram/Vercel-отчет и документация: `scripts/lib/analytics-report.js`, `api/daily-analytics.js`, `api/telegram.js`, `scripts/send-daily-analytics.js`, `scripts/test-analytics-report.js`, `package.json`, README, `docs/telegram-bot-guide.md` и `docs/commit-log.md`. Код Figma-плагина, `src/code.ts`, `dist/code.js`, UI, manifest, типографические правила и уведомления обработки не менялись. Для применения нужен redeploy Vercel; обновлять установленный Figma-плагин из-за этой правки не нужно.
  Проверки: `node --check scripts/lib/analytics-report.js`, `node --check scripts/send-daily-analytics.js`, `node --check api/daily-analytics.js`, `node --check api/telegram.js`, `node --check scripts/test-analytics-report.js`, `npm test`, `git diff --check`.
- `975666b` — `Переименовать команду продвинутых фич`.
  Команда открытия интерфейса в `manifest.json` переименована с `⚙️ Настройки` на `⭐ Продвинутые фичи`, чтобы в меню Figma лучше отделять быстрый запуск типографа от расширенных режимов и настроек. Внутренний command id `open-settings` не менялся, поэтому логика открытия UI осталась прежней.
  README обновлен в актуальных описаниях команд и производительности под новое название. Исторические записи в журнале коммитов не переписывались.
  Затронуты только `manifest.json` и README. Код плагина, `dist/code.js`, UI-тексты внутри окна, типографические правила, уведомления, аналитика и Telegram/Vercel не менялись.
  Проверки: валидность `manifest.json` через `JSON.parse`, `git diff --check`. В Figma после обновления нужно импортировать/обновить plugin manifest и проверить, что в меню рядом с `Запустить типограф` отображается `⭐ Продвинутые фичи`.
- `e3f0e66` — `Починить отправку аналитики`.
  Исправлена отправка событий в PostHog: из Capture payload убрано кастомное поле `uuid`, потому что PostHog принимает только отсутствующий `uuid` или настоящий UUID-формат, а внутренние id вида `evt_...` приводили к `400 Bad Request`. Внутренний `eventId` оставлен только как id записи локальной очереди. Чтение очереди `analyticsEventQueue` теперь нормализует legacy-payload перед отправкой, чтобы старые события с плохим `uuid` не блокировали новые события.
  Аналитика больше не задерживает запуск из окна настроек и закрытие UI: ожидание отправки событий убрано из UI-сценариев. Для быстрого запуска оставлен короткий grace period `500 ms` перед закрытием плагина. При обработке выделенного текста, фрейма, группы или другого контейнера плагин больше не вызывает `figma.currentPage.loadAsync()` заранее; загрузка текущей страницы осталась только для сценария без выделения, когда нужно обработать всю страницу.
  README и `docs/analytics-plan.md` обновлены: зафиксированы формат PostHog payload без кастомного `uuid`, локальная очередь аналитики и условная загрузка страницы. Добавлен регрессионный тест, что `createAnalyticsEventPayload` не содержит `uuid`; `dist/code.js` пересобран.
  Затронуты `src/code.ts`, `scripts/test-clean-typography.js`, сгенерированный `dist/code.js`, README и `docs/analytics-plan.md`. `cleanTypography`, порядок типографических правил, тексты уведомлений, UI-тексты, manifest, Telegram/Vercel-код не менялись.
  Проверки: `npm test`, `git diff --check`, ручная диагностическая отправка тестового события `is_test_event: true` в PostHog вернула `200 OK`. В Figma после обновления нужно проверить быстрый запуск, запуск из настроек по выделенному фрейму, запуск без выделения по странице, появление уведомлений успеха/ошибки и появление реальных событий в PostHog/Telegram-отчете после деплоя.
- `79377a4` — `Исправить сокращения и сохранение style id`.
  Уточнены правила составных сокращений: кириллические сокращения `т. е.`, `т. к.`, `т. д.`, `т. п.`, `н. в.`, `и т. д.`, `и т. п.` и `в т. ч.` теперь требуют явный разделитель между частями — точку или пробел. Поэтому слитные слова и аббревиатуры вроде `Те`, `ТК` и `НВ` больше не раскрываются в сокращения. Если сокращение распознано, регистр кириллических букв сохраняется: `Т е` -> `Т. е.`, `т к` -> `т. к.`. Постскриптум оставлен отдельным эталонным правилом: `PS` и `PPS` приводятся к `P. S.` и `P. P. S.`.
  Дополнительно сохранение оформления стало надежнее для style id: `captureTextStyles` теперь добирает `textStyleId` и `fillStyleId` из range/node fallback и посимвольной проверки, а восстановление применяет `textStyleId` перед `fillStyleId`. Это нужно, чтобы после изменения `textNode.characters` не терять подключенные текстовые и цветовые стили.
  Затронуты `src/code.ts`, сгенерированный `dist/code.js` и `scripts/test-clean-typography.js`. README, UI-тексты, уведомления, настройки запуска, manifest, аналитика, Telegram/Vercel и правила вне блока сокращений не менялись.
  Проверки: `npm test`, `git diff --check`, ручная проверка фактических результатов `ТК`, `НВ`, `т к`, `н в`, `PS`, `PPS`. В Figma после обновления нужно проверить текстовые слои с `Те`, `т.е.`, `т е`, `ТК`, `НВ`, `PS`, `PPS`, а также слой с подключенными text style и fill style после запуска типографа.
- `651dbd7` — `Добавить опцию звездочек в Telegram-отчет`.
  В ежедневный Telegram-отчет и ответ команды `/today` добавлена новая строка в блок `Опции`: `— с перекраской звездочек`. Метрика считается по событиям `plugin_run_started`, где `properties.recolor_existing_asterisks = true`, то есть по запускам, в которых был выбран чекбокс `Считать все одиночные * пробелами`.
  Затронут только `scripts/lib/analytics-report.js`: UI плагина, правила типографики, `src/code.ts`, `dist/code.js`, README, Vercel endpoints и Telegram webhook не менялись.
  Проверки: `node --check scripts/lib/analytics-report.js`, `node --check scripts/send-daily-analytics.js`, `node --check api/daily-analytics.js`, `node --check api/telegram.js`, ручная проверка форматирования сообщения на тестовых данных, `git diff --check`. После push нужно дождаться redeploy Vercel и повторить команду `/today`: строка должна появиться даже при нулевом значении.
- `5950441` — `Уточнить правила звездочек и формул`.
  Изменено правило кавычек: многоточие больше не переносится через закрывающую кавычку и остается там, где стояло до обработки, внутри кавычек или после них.
  Добавлена настройка вкладки `Типограф` — `Считать все одиночные * пробелами`. Она доступна только в режиме `Для разработки`: безопасные одиночные пользовательские `*` временно считаются пробельными кандидатами, после чего типограф решает, заменить их обычным пробелом или служебным красным маркером неразрывного пробела. Серии звездочек, маски карт/паролей, латинские коды, email-like токены и небезопасные случаи остаются пользовательскими.
  Уточнены безопасные сценарии для существующих `*`: пробелы между кириллическими словами, внутри скобок, рядом с тире и между цифрой и кириллическим словом. Защищены кейсы `карта ****4444`, `карта****4444`, `SALE*2026` и `SALE*2*026`.
  Математика с умножением теперь распознает компактную формулу перед запятой: `2*2,` превращается в `2 × 2,`, а в режиме разработки — в `2*×*2,`.
  Высота окна настроек после добавления новой галочки уменьшена до `372px`, чтобы вкладка `Типограф` помещалась без скролла и без большой пустой зоны между настройками и кнопкой.
  Затронуты `src/code.ts`, сгенерированный `dist/code.js`, `scripts/test-clean-typography.js`, `src/ui-content.js`, сгенерированный `src/ui.html`, `scripts/sync-ui-content.js`, `preview/ui-preview.html`, README и `docs/analytics-plan.md`.
  Проверки: `npm test`, `git diff --check`, локальный headless Chrome screenshot `preview/ui-preview-tight.png`. В Figma после обновления нужно проверить режим `Для разработки` с включенной галочкой на примерах `Что*нужно`, `или*их*комбинации`, `Москва*—*столица`, `Есть блок (новая*настройка)`, `Кампания SALE*2*026`, `карта ****4444`, а также формулы `Формула 2*2=4` и `2*2,`.
- `8eac890` — `Оставлять окно настроек открытым после запуска`.
  При запуске типографа из окна `⚙️ Настройки` UI больше не закрывается после обработки: пользователь может сразу запустить следующую обработку и закрывает окно сам крестиком.
  Быстрый запуск `Запустить типограф` не менялся: команда по-прежнему работает без UI и закрывает плагин после обработки через общий `finally`.
  В обработчике сообщения `run-typograph` удален вызов `figma.closePlugin()` после `runTypograph(...)`; ожидание аналитики перед продолжением оставлено. README обновлен описанием нового поведения.
  Затронуты `src/code.ts`, сгенерированный `dist/code.js` и README. Правила типографики, вкладка `Правила`, тестовые ожидания, аналитика, обработка ошибок, уведомления, Telegram/Vercel и `manifest.json` не менялись.
  Проверки: `npm test`, `git diff --check`. В Figma после обновления нужно проверить запуск из настроек в режимах `Для красоты` и `Для разработки`: после уведомления окно должно оставаться открытым; быстрый запуск должен закрываться как раньше.
- `aef35f2` — `Добавить неразрывный пробел перед процентами`.
  Добавлено whitelist-правило для неразрывного пробела между показателем и процентным значением: `скидка 5%` -> `скидка 5%`, `со скидкой 7%` -> `со скидкой 7%`, `кэшбэк 10%` -> `кэшбэк 10%`, `НДС 20%` -> `НДС 20%`.
  Whitelist учитывает формы слов без морфологического анализатора: `скидка`, `кешбэк/кэшбэк`, `ставка`, `комиссия`, `доходность`, `рассрочка`, `налог`, `НДС` и их согласованные склонения. Правило матчится только по целому слову: `антискидка 5%`, `супер-скидка 5%`, `sale 5%` и любые произвольные слова перед процентом не связываются.
  Правило также связывает показатель с компактным процентным диапазоном: `скидка 5-10%` -> `скидка 5—10%`, при этом сам диапазон остается без пробелов вокруг длинного тире. Сценарий `100% - 7%` защищен как математическое выражение и нормализуется в `100% − 7%`, а не в диапазон.
  Обновлена вкладка `Правила`, секция `Неразрывные пробелы`: добавлена строка про слова из whitelist и процент, а строка про частицы уточнена как `ли/ль`, `же/ж`, `бы/б`. Для выделений использован стандартный стиль `rule-example`, без отдельного `strong`.
  Затронуты `src/code.ts`, сгенерированный `dist/code.js`, `scripts/test-clean-typography.js`, `src/ui-content.js`, сгенерированный `src/ui.html` и README. Аналитика, обработка ошибок, уведомления, Telegram/Vercel и `manifest.json` не менялись.
  Проверки: `npm test`, `git diff --check`. В Figma после обновления нужно проверить быстрый запуск на фразах `со скидкой 7%`, `ставка 5—10%`, `оборот 7%`, `антискидка 5%`, `100% - 7%` и вкладку `Правила` -> `Неразрывные пробелы`.
- `7d76e4a` — `Обновить правила диапазонов`.
  Диапазоны переведены на длинное тире `—`: простые компактные значения оформляются без пробелов (`5—10 кг`, `2—5 ноября`, `12:25—13:35`, `30.04—12.05`, `X—XI век`), а составные границы оформляются с пробелами вокруг длинного тире и общим неразрывным пробелом перед ним (`2 000 — 4 000`, `1 января — 15 августа`, `02.02.2012 — 05.05.2013`, `2016 — н. в.`).
  Добавлены отдельные правила для процентов и температур: процент в диапазоне не дублируется слева и справа (`12,5%‑15,75%` -> `12,5—15,75%`), а температурные диапазоны со знаками разделяются многоточием (`+5...+10 °C` -> `+5…+10 °C`, `-5...+10 °C` -> `−5…+10 °C`).
  Защищены соседние сценарии: математические отрицательные числа остаются математикой, телефоны и номера не превращаются в диапазоны, дефисы внутри слов и кодов не ломаются, маскированные пароли вроде `****-1234` и `****−1*234` не нормализуются как числа.
  Добавлено составное сокращение `н. в.` с неразрывным пробелом. Обновлены README и вкладка `Правила`: секции `Тире и дефисы`, `Сокращения`, `Математика и символы`.
  Затронуты `src/code.ts`, сгенерированный `dist/code.js`, `scripts/test-clean-typography.js`, `src/ui-content.js`, сгенерированный `src/ui.html` и README. Аналитика, обработка ошибок, уведомления, Telegram/Vercel и `manifest.json` не менялись.
  Проверки: `npm test`, `git diff --check`. В Figma после обновления нужно проверить быстрый запуск на диапазонах дат/времени/чисел/римских цифр, составных диапазонах, процентах, температурах со смешанными знаками, маскированных паролях и вкладку `Правила`.
- `0588515` — `Восстановить дефисы в устойчивых словах`.
  Добавлено восстановление дефисов в устойчивых сочетаниях, если пользователь написал их через пробел: `из за` -> `из‑за`, `из под` -> `из‑под`, `кто то` -> `кто‑то`, `что либо` -> `что‑либо`, `где нибудь` -> `где‑нибудь`, `кое как` -> `кое‑как`, `все таки` -> `все‑таки`, `всё таки` -> `всё‑таки`.
  Новое правило работает внутри блока `Тире и дефисы` перед общей заменой дефиса внутри слов на неразрывный дефис, поэтому итоговый дефис в этих словах сразу становится неразрывным.
  Обновлена вкладка `Правила`, секция `Тире и дефисы`: строка теперь звучит как `Неразрывный дефис в словах: из-за, из-под, кто-то, что-либо, где-нибудь, кое-как, все-таки, всё-таки`.
  Затронуты `src/code.ts`, сгенерированный `dist/code.js`, `scripts/test-clean-typography.js`, `src/ui-content.js`, сгенерированный `src/ui.html` и README. Аналитика, обработка ошибок, уведомления, Telegram/Vercel и `manifest.json` не менялись.
  Проверки: `npm test`, `git diff --check`. В Figma после обновления нужно проверить быстрый запуск и запуск из настроек на фразах `из за угла`, `из под стола`, `кто то пришёл`, `что либо ещё`, `где нибудь рядом`, `кое как сделали`, `все таки получилось`, `всё таки получилось`, а также вкладку `Правила` -> `Тире и дефисы`.
- `35eefe9` — `Расширить правила сокращений`.
  Расширен список сокращений с точкой: добавлены `гл.`, `илл.`, `ст.`, `п.`, `см.`, `обл.`, `кр.`, `пос.`, `пер.`, `пр.`, `просп.`, `пл.`, `бул.`, `наб.`, `ш.`, `туп.`, `оф.`, `комн.`, `под.`, `мкр.`, `уч.`, `вл.`, `влад.`, `корп.`, `эт.`, `пгт.`. Для `под.` добавлена защита от ложного срабатывания на предлог: `под 2` становится `под. 2`, но `под столом` не меняется.
  Добавлены сокращения без точки `б-р` и `пр-т`: лишняя точка после них удаляется, а дефис внутри сокращения нормализуется в неразрывный дефис. Одновременно уточнено правило частиц `б`, `ж`, `ль`: оно больше не срабатывает, если после частицы стоит обычный или неразрывный дефис, поэтому `б-р` не получает неразрывный пробел как частица `б`.
  Добавлены составные сокращения с неразрывными пробелами: `P. S.`, `P. P. S.`, `в т. ч.`.
  Обновлена вкладка `Правила`, секция `Сокращения`: отдельно показаны сокращения с точкой, составные сокращения и сокращения без точки. README обновлен теми же правилами.
  Затронуты `src/code.ts`, сгенерированный `dist/code.js`, `scripts/test-clean-typography.js`, `src/ui-content.js`, сгенерированный `src/ui.html` и README. Аналитика, обработка ошибок, уведомления, Telegram/Vercel и `manifest.json` не менялись.
  Проверки: `npm test`, `git diff --check`. В Figma после обновления нужно проверить быстрый запуск на адресных сокращениях (`пр-т`, `б-р`, `просп`, `оф`, `под 2`, `под столом`), составных сокращениях (`P.S.`, `P P S`, `в т ч`) и вкладку `Правила` -> `Сокращения`, чтобы длинные строки читались без переполнения.
- `7aeced7` — `Обновить строку про частицы в правилах`.
  В секции `Неразрывные пробелы` на вкладке `Правила` обновлена строка про частицы по согласованной формулировке: `Перед частицами ли, же, бы: далеко*ли, можно*ль, он*же, всё*ж красиво, сказал*бы, знал*б ты`.
  Изменение касается только UI-текста: обновлен источник `src/ui-content.js` и сгенерированный `src/ui.html`.
  Код типографики, тестовые ожидания, `dist/code.js`, README, уведомления, аналитика, обработка ошибок, Telegram/Vercel и `manifest.json` не менялись.
  Проверки: `npm run build`, `git diff --check`. В Figma после обновления нужно открыть `⚙️ Настройки` -> `Правила` -> `Неразрывные пробелы` и проверить, что новая длинная строка читается без переполнения.
- `c3324dd` — `Добавить короткие частицы к неразрывным пробелам`.
  Расширено существующее правило частиц: неразрывный пробел теперь ставится не только перед `ли`, `же`, `бы`, но и перед короткими формами `ль`, `ж`, `б`. Примеры: `можно ль` -> `можно ль`, `всё ж красиво` -> `всё ж красиво`, `знал б ты` -> `знал б ты`.
  Защита правила коротких слов обновлена на те же частицы: после `ль`, `ж`, `б` не ставится дополнительный неразрывный пробел, как уже было для `ли`, `же`, `бы`.
  Добавлены тесты для режима `Для красоты`, проверки начала строки `ль стало понятно` и повторного запуска режима `Для разработки`, где новые неразрывные пробелы отображаются звездочками.
  Обновлен README в разделе `Неразрывные пробелы`. Вкладка UI `Правила`, `src/ui-content.js`, `src/ui.html`, уведомления, аналитика, обработка ошибок, Telegram/Vercel и `manifest.json` не менялись.
  Затронуты `src/code.ts`, сгенерированный `dist/code.js`, `scripts/test-clean-typography.js` и README.
  Проверки: `npm test`, `git diff --check`. В Figma после обновления нужно проверить быстрый запуск и запуск из настроек на фразах `можно ль иначе`, `всё ж красиво`, `знал б ты`, а также режим `Для разработки`, где результат должен быть `можно*ль иначе`, `всё*ж красиво`, `знал*б ты`.
- `840ae95` — `Задокументировать план UI-отчета об ошибках`.
  Зафиксирован новый план обработки ошибок: если ошибка относится к конкретному текстовому слою, плагин должен продолжать обработку остальных слоев, а затем показывать пользователю UI-отчет со списком проблемных слоев слева и подсказкой справа. Основной идентификатор строки — `textPreview`; название слоя можно показывать вторично мелким серым текстом. Для `single_text` используется тот же отчет с одним элементом в списке. Для общей ошибки запуска предусмотрен отдельный сценарий без привязки к текстовому слою.
  В отдельный документ вынесена дизайн-система будущего окна ошибок: заголовки, кегли, цвета, заливки, компактные отступы, активная строка без рамки, кнопка `Показать в макете`, запрет на новые красные/синие состояния и требование максимально повторять стили текущего плагина. Важное правило: блок с деталями ошибки должен быть компактным, с отступами как во вкладке `Правила`, без лишних линий, бейджей и вторичных кнопок.
  Отложенная идея PostHog/Telegram-уведомлений вынесена в `docs/error-monitoring.md`: сохранены согласованные тексты типологии ошибок, будущие поля аналитики и возможная команда `/error_types`, но это не является текущей продакшен-задачей.
  Добавлено статичное превью `preview/error-report-preview.html`, чтобы сначала согласовывать внешний вид окна в браузере, а уже потом переносить механику в Figma-плагин.
  Обновлены README и `docs/telegram-bot-guide.md` ссылками на новую документацию и пометкой, что справочник ошибок для Telegram пока отложен.
  Код плагина не менялся: `src/code.ts`, `dist/code.js`, `src/ui.html`, `src/ui-content.js`, `manifest.json`, правила типографики, порядок замен, тестовые ожидания, уведомления, аналитика и Telegram endpoint не затронуты. Критическое требование на следующий этап: при реализации UI-отчета не менять сами правила типографики и их результаты.
  Проверки: `git diff --check`. `npm test` не запускался, потому что изменения касаются только документации и статичного HTML-превью.
- `a5e3e36` — `Перенести ежедневный отчет на Vercel Cron`.
  Добавлен Vercel endpoint `api/daily-analytics.js`: он принимает `GET` от Vercel Cron, проверяет заголовок `Authorization: Bearer <CRON_SECRET>`, собирает отчет за вчера через общую функцию `createAnalyticsMessage("yesterday")` и отправляет сообщение в Telegram.
  В `vercel.json` добавлен cron `/api/daily-analytics` с расписанием `0 6 * * *`, что соответствует `09:00` по Москве. На Hobby-тарифе Vercel может выполнить cron в пределах часа.
  Из `.github/workflows/daily-analytics.yml` убрано расписание `schedule`; `workflow_dispatch` оставлен как ручной резервный запуск, чтобы не получать дубли ежедневных сообщений.
  README и `docs/telegram-bot-guide.md` обновлены: ежедневный отчет теперь описан как Vercel Cron, добавлен `CRON_SECRET` и инструкция по генерации секрета.
  Код Figma-плагина не менялся: правила типографики, UI и логика обработки не затрагивались. Отдельно важно: в рабочем дереве до этой задачи уже были незакоммиченные изменения в `src/code.ts`, `dist/code.js`, README, `docs/telegram-bot-guide.md` и `scripts/lib/analytics-report.js`; эта запись описывает только Vercel Cron-часть.
  Проверки: `node --check api/daily-analytics.js`, `node --check api/telegram.js`, `node --check scripts/lib/analytics-report.js`, `node --check scripts/send-daily-analytics.js`, `npm test`, `git diff --check`.
- `2d2788e` — `Сохранять списки при обработке текста`.
  Исправлено сохранение форматирования списков после замены `textNode.characters`: плагин теперь сохраняет и восстанавливает по диапазонам `listOptions`, `listSpacing`, `indentation`, `paragraphIndent` и `paragraphSpacing`, поэтому маркированные и нумерованные списки не должны превращаться в обычный текст после типографики.
  README уточнен: в списке сохраняемого форматирования теперь явно указаны списки, отступы и абзацные интервалы.
  Добавлен регрессионный тест для восстановления list/paragraph-свойств через `restoreTextStyles`.
  Правила типографики, порядок замен, уведомления, настройки запуска, UI-тексты вкладок, аналитика, Telegram-отчеты и `manifest.json` не менялись.
  Затронуты `src/code.ts`, сгенерированный `dist/code.js`, `scripts/test-clean-typography.js` и README.
  Проверки: `npm test`, `git diff --check`. В Figma после обновления нужно проверить маркированный и нумерованный списки, включая вложенные пункты: после быстрого запуска и запусков из настроек список, маркеры/нумерация и отступы должны сохраниться.
- `8c4a7c5` — `Уточнить формат Telegram-отчетов`.
  В общий форматтер Telegram-отчетов добавлен жирный заголовок через HTML-разметку Telegram: `✦ Чистовик сегодня` для команды `/today` и `✦ Чистовик за <дата>` для ежедневного отчета.
  Ссылка на PostHog теперь отправляется как кликабельный текст `Полный дашборд с графиками`, URL не показывается в сообщении. Пометка `(открывается только с vpn)` остается рядом со ссылкой обычным текстом.
  `sendTelegramMessage` теперь отправляет сообщения с `parse_mode: "HTML"` и выключенным preview ссылок. Динамические куски текста экранируются перед вставкой в HTML.
  README и `docs/telegram-bot-guide.md` обновлены под новый формат сообщений.
  Код Figma-плагина не менялся: `src/code.ts`, `dist/code.js`, `src/ui.html`, `src/ui-content.js`, `manifest.json`, правила типографики, UI и уведомления не затронуты.
  Проверки: `node --check scripts/lib/analytics-report.js`, `node --check scripts/send-daily-analytics.js`, `node --check api/telegram.js`, `npm test`, `git diff --check`. После push нужно дождаться redeploy Vercel и проверить команду `/today`, а ежедневный отчет обновится после следующего запуска GitHub Actions.
- `89f6a7d` — `Добавить гайд по Telegram-боту`.
  Добавлен `docs/telegram-bot-guide.md` с рабочими инструкциями по Telegram-боту: где лежит код, какие GitHub Secrets и Vercel Environment Variables нужны, как узнать `TELEGRAM_CHAT_ID`, как сгенерировать `TELEGRAM_WEBHOOK_SECRET`, как установить/проверить/удалить webhook, как добавить команду `/today` в меню бота, как посмотреть и очистить команды, как вручную проверить ежедневный отчет и что делать, если `/today` не отвечает.
  README дополнен ссылкой на новый гайд и строкой в структуре проекта.
  Код Figma-плагина, Vercel endpoint, GitHub Actions workflow и логика отчетов не менялись.
  Проверки: `git diff --check`. После push гайд будет доступен в репозитории как рабочая памятка для будущих настроек Telegram-бота.
- `1aaca25` — `Добавить public output для Vercel`.
  Добавлена минимальная страница `public/index.html` и в `vercel.json` указан `outputDirectory: "public"`, потому что Vercel с preset `Other` отклонял deploy после `buildCommand: echo "No build needed"` с ошибкой отсутствующей папки `public`.
  Serverless endpoint `api/telegram.js` остается рабочей частью проекта для Telegram webhook; `public/index.html` нужен только как статический output для успешного деплоя Vercel.
  Код Figma-плагина не менялся: `src/code.ts`, `dist/code.js`, `src/ui.html`, `src/ui-content.js`, `manifest.json`, правила типографики, UI и уведомления не затронуты.
  Проверки: валидность `vercel.json` через `JSON.parse`, `npm test`, `git diff --check`. После push нужно повторить deploy в Vercel.
- `4354ddd` — `Упростить Vercel-конфиг endpoint`.
  Из `vercel.json` убрано явное поле `functions.api/telegram.js.runtime`, потому что Vercel отклонил значение `nodejs20.x` с ошибкой `Function Runtimes must have a valid version`. Vercel должен сам определить runtime для `api/telegram.js` как Node.js serverless function.
  В `vercel.json` оставлен только `buildCommand: echo "No build needed"`, чтобы Vercel не запускал сборку Figma-плагина и не искал статическую папку `public`.
  Код Figma-плагина не менялся: `src/code.ts`, `dist/code.js`, `src/ui.html`, `src/ui-content.js`, `manifest.json`, правила типографики, UI и уведомления не затронуты.
  Проверки: валидность `vercel.json` через `JSON.parse`, `npm test`, `git diff --check`. После push нужно повторить deploy в Vercel.
- `819e082` — `Настроить Vercel для Telegram endpoint`.
  Добавлен `vercel.json`, чтобы Vercel не запускал сборку Figma-плагина и не искал статическую папку `public` при деплое проекта, который нужен только для serverless endpoint `api/telegram.js`.
  В конфиге задан `buildCommand: echo "No build needed"` и runtime `nodejs20.x` для `api/telegram.js`.
  Код Figma-плагина не менялся: `src/code.ts`, `dist/code.js`, `src/ui.html`, `src/ui-content.js`, `manifest.json`, правила типографики, UI и уведомления не затронуты.
  Проверки: валидность `vercel.json` через `JSON.parse`, `npm test`, `git diff --check`. После push нужно повторить deploy в Vercel.
- `7fe2121` — `Добавить команду today для Telegram-бота`.
  Добавлена основа для Telegram-команды `/today` через Vercel endpoint `api/telegram.js`. Endpoint принимает webhook от Telegram, проверяет `x-telegram-bot-api-secret-token`, разрешает ответы только для `TELEGRAM_CHAT_ID`, игнорирует чужие сообщения и неизвестные команды, а на `/today` отправляет отчет за текущий день по Москве.
  Общая логика PostHog-запросов, расчета периода по Москве, форматирования сообщения и отправки в Telegram вынесена в `scripts/lib/analytics-report.js`. Ежедневный GitHub Actions отчет теперь использует тот же модуль через тонкий скрипт `scripts/send-daily-analytics.js`.
  В ежедневный отчет и отчет по `/today` добавлена ссылка: `Полный дашборд с графиками (открывается только с vpn)` и URL `https://eu.posthog.com/project/184090/dashboard/695809`. Для GitHub Actions ссылка задана через обычный env `POSTHOG_DASHBOARD_URL`; в общем модуле есть такой же дефолт.
  README дополнен инструкциями для Vercel: переменные окружения `POSTHOG_PERSONAL_API_KEY`, `POSTHOG_HOST`, `POSTHOG_PROJECT_ID`, `POSTHOG_DASHBOARD_URL`, `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`, `TELEGRAM_WEBHOOK_SECRET`, а также шаблон ссылки `setWebhook`.
  Код Figma-плагина не менялся: `src/code.ts`, `dist/code.js`, `src/ui.html`, `src/ui-content.js`, `manifest.json`, правила типографики, UI и уведомления не затронуты.
  Затронуты `api/telegram.js`, `scripts/lib/analytics-report.js`, `scripts/send-daily-analytics.js`, `.github/workflows/daily-analytics.yml`, README и `docs/commit-log.md`.
  Проверки: `node --check scripts/lib/analytics-report.js`, `node --check scripts/send-daily-analytics.js`, `node --check api/telegram.js`, `npm test`, `git diff --check`. После push нужно задеплоить проект на Vercel, добавить env vars, установить Telegram webhook и проверить команду `/today`.
- `b7672ba` — `Уточнить ежедневный Telegram-отчет`.
  В ежедневный Telegram-отчет добавлены две строки: `Без финального статуса` и `— мультивыбор` в блоке `Область`.
  `Без финального статуса` считается как `plugin_run_started - plugin_run_completed - plugin_run_failed`, чтобы было видно, сколько запусков получили стартовое событие, но не получили финальное событие успеха или ошибки. Это помогает не путать потерянные финальные события аналитики с ошибками типографа.
  `мультивыбор` считается по `selection_scope = 'multi_selection'`, чтобы сумма областей запуска сходилась с общим числом запусков типографа.
  Затронуты `scripts/send-daily-analytics.js`, README и `docs/commit-log.md`. Код Figma-плагина, правила типографики, `src/code.ts`, `dist/code.js`, UI и уведомления не менялись.
  Проверки: `node --check scripts/send-daily-analytics.js`, `npm test`, `git diff --check`. После push нужно вручную запустить workflow `Daily analytics report` и проверить, что в сообщении появились строки `Без финального статуса` и `— мультивыбор`.
- `01de8ba` — `Добавить ежедневный Telegram-отчет`.
  Добавлен ежедневный отчет по PostHog-аналитике в Telegram через GitHub Actions. Workflow `.github/workflows/daily-analytics.yml` запускается каждый день в `06:00 UTC`, что соответствует `09:00` по Москве, и поддерживает ручной запуск через `workflow_dispatch`.
  Добавлен скрипт `scripts/send-daily-analytics.js`: он считает предыдущий календарный день по `Europe/Moscow`, запрашивает PostHog Query API/HogQL, исключает тестовые события с `is_test_event: true`, подставляет нули при отсутствии данных и отправляет русское сообщение в Telegram через Bot API.
  Отчет включает уникальных пользователей, запуски типографа, успешные обработки, ошибки, разбивку по режимам (`default`, `beauty`, `development`), область запуска (`single_text`, `container`, `page`), опции скрытых и заблокированных слоев, открытия настроек и переходы в канал.
  Секреты не хранятся в коде: workflow ожидает `POSTHOG_PERSONAL_API_KEY`, `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID` из GitHub Actions Secrets. Обычные настройки `POSTHOG_HOST: https://eu.posthog.com` и `POSTHOG_PROJECT_ID: "184090"` зафиксированы в workflow.
  В `package.json` добавлена команда `npm run send-daily-analytics`, README дополнен разделом `Ежедневный Telegram-отчет`.
  Правила типографики и код Figma-плагина не менялись: `src/code.ts`, `dist/code.js`, `src/ui.html`, `src/ui-content.js`, уведомления и тестовые ожидания не затронуты.
  Затронуты `.github/workflows/daily-analytics.yml`, `scripts/send-daily-analytics.js`, `package.json`, README и `docs/commit-log.md`.
  Проверки: `node --check scripts/send-daily-analytics.js`, `npm test`, `git diff --check`. После push нужно открыть GitHub Actions, вручную запустить workflow `Daily analytics report` через `Run workflow` и проверить сообщение в Telegram.
- `1dc7c2a` — `Добавить анонимную аналитику запусков`.
  Добавлена анонимная техническая аналитика через PostHog Cloud EU. Плагин отправляет события `settings_opened`, `plugin_run_started`, `plugin_run_completed`, `plugin_run_failed` и `channel_link_clicked`.
  Для событий запуска передаются источник запуска (`quick_run` или `settings`), режим (`default`, `beauty`, `development`), флаги обработки замочков и скрытых слоев, тип области запуска (`single_text`, `container`, `page`, `multi_selection`), счетчики найденных, обработанных, измененных, пропущенных и упавших текстовых нод, длительность обработки, версия плагина и версия схемы аналитики.
  Уникальность пользователя считается по анонимному id из `figma.clientStorage`; Figma user id, email, имя пользователя, текст макета, названия файлов, страниц и слоев не отправляются. В каждое событие добавлено `$geoip_disable: true`, чтобы PostHog не добавлял город, страну, координаты и другие данные по IP.
  В `manifest.json` вместо `networkAccess.allowedDomains: ["none"]` разрешен только `https://eu.i.posthog.com`, добавлена причина сетевого доступа. Ошибки аналитики гаснут внутри аналитического слоя и не показываются пользователю. Перед закрытием плагина добавлено короткое ожидание до 1000 мс, чтобы финальные события `plugin_run_completed` и `plugin_run_failed` успевали отправиться; обработка текста и уведомления не ждут аналитику.
  Клик по ссылке на канал на вкладке `О плагине` теперь отправляет событие `channel_link_clicked`; сама ссылка продолжает работать как обычная ссылка.
  README сокращен: полный журнал коммитов вынесен в `docs/commit-log.md`, а в README оставлена ссылка и правило вести будущие записи здесь. Добавлен `docs/analytics-plan.md` с согласованным планом аналитики и жестким требованием не менять типографские правила.
  Правила типографики не менялись: `cleanTypography`, порядок замен, уведомления, вкладка `Правила`, `src/ui-content.js` и тестовые ожидания в `scripts/test-clean-typography.js` содержательно не тронуты.
  Затронуты `src/code.ts`, сгенерированный `dist/code.js`, `src/ui.html`, `manifest.json`, README, `docs/analytics-plan.md` и `docs/commit-log.md`.
  Проверки: `npm test`, `git diff --check`, валидность `manifest.json` через `JSON.parse`, ручная проверка событий в PostHog. В PostHog проверены быстрый запуск, запуск из настроек, режимы `beauty` и `development`, один текстовый слой, фрейм/контейнер, вся страница, замочки, скрытые слои, клик по каналу и отключение GeoIP.
  В Figma после обновления нужно проверить быстрый запуск по одному текстовому слою, запуск из настроек в режимах `Для красоты` и `Для разработки`, запуск по фрейму, запуск без выделения по всей странице, чекбоксы замочков и скрытых слоев, а также убедиться в PostHog, что `plugin_run_completed` приходит с корректными properties.
- `217e576` — `Добавить неразрывный пробел перед частицами`.
  Добавлено правило неразрывного пробела перед частицами `ли`, `же`, `бы`: `далеко ли` -> `далеко ли`, `он же` -> `он же`, `сказал бы` -> `сказал бы`. В режиме `Для разработки` эти пробелы отображаются звездочками: `далеко*ли`, `он*же`, `сказал*бы`.
  Исправлено взаимодействие с правилом коротких слов: после частиц `ли`, `же`, `бы` не ставится дополнительный неразрывный пробел, но следующее короткое слово не пропускается. Кейс `Это же не баг, а фича ли?` теперь становится `Это же не баг, а фича ли?`, а в режиме `Для разработки` — `Это*же не*баг, а*фича*ли?`.
  Обновлена вкладка `Правила`, секция `Неразрывные пробелы`: добавлена строка `Перед ли, же, бы: далеко*ли, он*же, сказал*бы`.
  Затронуты `src/code.ts`, сгенерированный `dist/code.js`, тесты `scripts/test-clean-typography.js`, README, `src/ui-content.js` и сгенерированный `src/ui.html`.
  Проверки: `npm test`, `git diff --check`. В Figma после обновления нужно проверить фразы `далеко ли холодно ли стало`, `он же сказал бы`, `Это же не баг, а фича ли?` в режимах `Для красоты` и `Для разработки`, а также новую строку на вкладке `Правила` -> `Неразрывные пробелы`.
- `4a06a93` — `Подготовить внутреннюю beta-версию`.
  С этой версии начинается бета-тестирование плагина на коллегах: beta-версия публикуется как private organization beta для внутренней проверки до публичного релиза.
  В `manifest.json` название изменено на `Чистовик — Internal Beta`, чтобы тестовая версия не смешивалась с будущей публичной версией. `id` заменен на `1638816840468168578`, добавлен явный запрет сетевого доступа через `networkAccess.allowedDomains: ["none"]`, потому что плагину не нужна сеть.
  В README добавлен блок `Beta-публикация и будущее`: перед публичной публикацией в Figma Community нужно будет заменить название, `id`, иконку, обложку и описание. Beta-название и beta-идентификаторы не должны переехать в публичный Community-релиз.
  Затронуты только `manifest.json` и README. Логика типографики, UI, уведомления, команды меню, `dist/code.js`, `src/ui-content.js` и `src/ui.html` не менялись.
  Проверки: валидность `manifest.json` через `JSON.parse`, `git diff --check`. В Figma после обновления нужно импортировать плагин из `manifest.json` и убедиться, что в Development-списке он отображается как `Чистовик — Internal Beta`.
- `bce84b6` — `Обновить стрелки в правилах и высоту окна`.
  Добавлено правило символов: комбинация `=>` теперь заменяется на `→`, как уже заменялась комбинация `->`. Добавлены тесты для `Статус => готово` и `Назад -> вперёд`.
  Обновлена вкладка `Правила`, только секция `Математика и символы`: строка `Стрелка` теперь показывает оба варианта `- >` и `= >` через узкий пробел между символами, чтобы в интерфейсе было видно отличие исходной комбинации от готовой стрелки. В строках `Стрелка` и `Умножение` союз `и` вынесен из жирного примера и отображается обычным серым текстом.
  Вкладка `Неразрывные пробелы` не менялась: новое числовое правило сознательно не добавлялось в UI по договоренности.
  Высота окна настроек увеличена с `340` до `360`, чтобы вкладка `Типограф` помещалась без вертикального скролла. Локальное превью `preview/ui-preview.html` обновлено на ту же высоту.
  Затронуты `src/code.ts`, сгенерированный `dist/code.js`, тесты `scripts/test-clean-typography.js`, README, `src/ui-content.js`, сгенерированный `src/ui.html` и `preview/ui-preview.html`.
  Проверки: `npm test`, `git diff --check`. В Figma после обновления нужно проверить, что вкладка `Типограф` открывается без вертикального скролла, а на вкладке `Правила` -> `Математика и символы` строки `Умножение` и `Стрелка` читаются без слипания.
- `fff7720` — `Добавить неразрывный пробел после обычных чисел`.
  Добавлено правило для обычных чисел перед следующим словом: `15 завтра` становится `15 завтра`, `10000 клиентов` становится `10 000 клиентов`, а в режиме `Для разработки` такие неразрывные пробелы отображаются звездочками.
  Правило не применяется к телефонам, номерам после `№`, параграфам после `§`, годам и числам внутри кодов. Если число уже связано слева числовым правилом, дополнительный неразрывный пробел после числа не ставится: `в д. 5 живёт` -> `в д. 5 живёт`, `г. 10000 жителей` -> `г. 10 000 жителей`.
  Неразрывный пробел слева от короткого слова не блокирует новое числовое правило: `По 2000 человек` -> `По 2 000 человек`. Для этого уточнена защита годов после `по`: `по 2000 человек` теперь считается количеством, а `по 2026 год` остается годом.
  Вкладка `Правила`, `src/ui-content.js`, `src/ui.html`, уведомления, настройки запуска и `manifest.json` не менялись по договоренности: тексты вкладки `Правила` в этом этапе не обновляем.
  Затронуты `src/code.ts`, сгенерированный `dist/code.js`, тесты `scripts/test-clean-typography.js` и README.
  Проверки: `npm test`, `git diff --check`. В Figma после обновления нужно проверить быстрый запуск и запуск через вкладку `Типограф` на фразах `Встреча 15 завтра`, `По 2000 человек`, `в д. 5 живёт`, `№ 10000 заявок`, `Позвоните +7 (900) 123-45-67 завтра`.
- `e915ffe` — `Исправить повторный режим разработки и сокращения`.
  Исправлена обработка пользовательской звездочки `*` в математике: в режиме `Для красоты` `2 * 2 = 4` снова превращается в `2 × 2 = 4`, а в режиме `Для разработки` — в `2*×*2*=*4`. Служебные development-маркеры больше не угадываются общим правилом; обычные `*` не удаляются и не превращаются в пробел.
  Для повторного запуска режима `Для разработки` добавлена ограниченная защита уже типографированных паттернов со звездочками: суммы, телефоны, длинные разрядные числа и годы рядом с `©` не попадают в математическое правило и не получают лишнее умножение. Отдельно защищены номера после `№` и `§`: `№ 79001234567` не становится телефоном, не получает `+7` и не разбивается на разряды, даже если между `№` и числом уже стоит development-маркер.
  Сужено правило неразрывного пробела после чисел: он ставится перед месяцами, единицами измерения и денежно-числовыми сокращениями, но не перед любым обычным словом. Вернулся неразрывный пробел между `©` и годом: `©2025` -> `© 2025`.
  Уточнены правила точек у сокращений без точки и единиц: точка сохраняется только перед новым предложением в той же строке; в конце строки или текста убирается (`5 кг.`, `5 млрд.`, `5000 ₽/мес.`). Для сокращений `д.`, `стр.`, `кв.` перед числом добавлен неразрывный пробел: `д. 5`, `стр. 10`, `кв. 7`.
  Повторная подкраска development-маркеров больше не считается текстовым изменением для уведомления. Если текст уже чистый, повторный запуск должен показывать уведомление `Всё уже было чисто 👌`, а не `Теперь всё чисто 🔥🔥🔥`.
  Обновлена вкладка `Правила`: только секция `Неразрывные пробелы`. В примерах этой секции неразрывные пробелы показаны красной звездочкой цвета `#FF4053`, добавлен пункт `©*2025`, в пункте про число и единицу есть пример `20*°C`. Другие секции вкладки `Правила`, уведомления как текстовые строки, настройки запуска и `manifest.json` не менялись.
  Затронуты `src/code.ts`, сгенерированный `dist/code.js`, тесты `scripts/test-clean-typography.js`, README, `src/ui-content.js` и сгенерированный `src/ui.html`.
  Проверки: `npm test`, `npm run build`, `git diff --check`. В Figma после обновления нужно проверить повторный запуск режима `Для разработки` 3-5 раз на суммах, телефонах, `№ 79001234567`, `© 2025`, сокращениях без точки в конце строки и вкладку `Правила` -> `Неразрывные пробелы`.
- `b8f1515` — `Исправить контекст вложенных кавычек`.
  Переписана логика форматирования кавычек: плагин больше не выбирает итоговый символ только по исходной кавычке. Сначала определяется роль кавычки в позиции (`opening` или `closing`), затем выбирается символ по языку локального контекста и уровню вложенности.
  Для верхнего уровня язык определяется по текущей строке и по тексту вне всей цитаты верхнего уровня. Вложенный текст больше не попадает обратно в языковой контекст, поэтому русский внешний текст с английской фразой внутри оформляется русской моделью: `Он сказал "Use 'clean typography' mode"` -> `Он сказал «Use „clean typography“ mode»`. Английский внешний контекст с русским словом внутри сохраняет английскую модель: `The word "привет" means hello`.
  Исправлены повторные запуски: правильно оформленные русские и английские типографские кавычки не ломаются, неправильное направление типографских кавычек исправляется по роли, а режим `Для разработки` учитывает `*` как маркер типографского пробела вокруг кавычек.
  Затронуты `src/code.ts`, сгенерированный `dist/code.js`, тесты `scripts/test-clean-typography.js` и README. Вкладка `Правила`, `src/ui-content.js`, `src/ui.html`, уведомления, настройки запуска и `manifest.json` не менялись.
  Проверки: `npm test`, `git diff --check`. В Figma после обновления нужно проверить русский контекст с английской цитатой (`Он сказал "Use 'clean typography' mode"`), английский контекст с русским словом (`The word "привет" means hello`) и повторный запуск режима `Для разработки` на уже оформленных вложенных кавычках.
- `504ecc2` — `Добавить математику и римские диапазоны`.
  Добавлена нормализация математических примеров: операторы `+`, `*`, `×`, `x`, `х`, `/`, `=` распознаются в выражениях с минимум двумя числами, вокруг бинарных операторов ставятся неразрывные пробелы, `*`, `x` и `х` приводятся к `×`, а дефис, среднее тире, длинное тире и существующий минус в вычитании приводятся к знаку минус `−`. Дефис или тире между двумя числами считается вычитанием только в математическом контексте: если есть `=`, другой математический оператор, больше одного оператора или унарный минус перед первым числом. Без такого контекста два числа остаются диапазоном: `5 - 5` -> `5–5`.
  Добавлена поддержка диапазонов с римскими цифрами в верхнем регистре в ожидаемом контексте: `X-XI век`, `I - III главы`, `главы I-III`, `разделы IV-VI`. Без контекста новое правило римских диапазонов не ставит среднее тире, но старое правило неразрывного дефиса внутри слов продолжает работать для кириллицы и латиницы.
  Добавлен тестовый запуск `npm test` и файл `scripts/test-clean-typography.js`, который проверяет математические примеры, конфликт с диапазонами, римские диапазоны, защищенные телефоны, даты, версии, IP, URL, email, переменные и промокоды, а также идемпотентность повторного запуска.
  Обновлена вкладка `Правила`: в `src/ui-content.js` и сгенерированном `src/ui.html` уточнены пункты `Тире и дефисы` и `Математика и символы`. README расширен описанием новых правил и проверок. Уведомления, команды меню, `manifest.json` и настройки запуска не менялись.
  Проверки: `npm test`, `git diff --check`. В Figma после обновления нужно проверить быстрый запуск `Запустить типограф`, запуск через вкладку `Типограф`, а на вкладке `Правила` раскрыть аккордеоны `Тире и дефисы` и `Математика и символы` и убедиться, что новые формулировки отображаются без переполнения.
- `5ec8dc3` — `Реализовать режим Для разработки`.
  Добавлен режим `Для разработки`: типографика применяется теми же правилами, но неразрывные пробелы отображаются как `*`, а типографские звездочки окрашиваются в `#FF4053`. Быстрый запуск остается в режиме `Для красоты`, UI передает выбранный режим через `PluginRunOptions`.
- `4885375` — `Исправить лишние звёздочки режима разработки`.
  Исправлен режим `Для разработки`: типографские `*` должны появляться только на местах неразрывных пробелов, пользовательские звездочки не должны перекрашиваться и не должны превращаться в `U+00A0` при возврате в режим `Для красоты`.
- `7d31fb4` — `Обновить логотип и акцентный цвет Чистовика`.
  Иконка приложения заменена на новый `assets/plugin-icon.svg`, акцентный цвет интерфейса изменен на `#065C5B`. Иконка не подключается через `manifest.json`: Figma задает ее при публикации или обновлении плагина.
- `b331a45` — `Уточнить заголовок режима типографа`.
  На вкладке `Типограф` заголовок изменен на `Как обработать текст?`, подписи режимов обновлены: `Для красоты` — `Готовыми символами`, `Для разработки` — `С * на месте неразрывных пробелов`. Кнопка запуска стала выше, с кеглем больше и `font-weight: 500`.
- `521a268` — `Собрать вкладку О плагине`.
  Добавлена полноценная вкладка `О плагине` в текущей стилистике интерфейса: крупный заголовок, список преимуществ с акцентными звездочками и авторская плашка.
- `7d49a60` — `Обновить текст вкладки О плагине`.
  Текст вкладки `О плагине` приведен к финальной версии: `Чистовик — это финальный штрих перед разработкой`, короткие пункты про кавычки, тире, пробелы, работу с текстом/фреймом/страницей и сохранение стилей. Авторская плашка получила цвет `#F0F4F3`, увеличенные внутренние отступы и ссылку `Заходите в гости →`.
- `87377c8` — `Добавить звёздочки на вкладку О плагине`.
  В правой верхней части авторской плашки добавлена декоративная композиция из акцентных звездочек. Положение несколько раз уточнялось: маленькие звездочки сближены, вся композиция опущена ниже, чтобы выглядеть единым декором.
- `4e45503` — `Оформить вкладку Правила`.
  Вкладка `Правила` сделана рабочей и оформлена аккордеонами. Текст правил сокращен по README и коду плагина, примеры выделены цветом без отдельных плашек. Неразрывные пробелы расставлены и в самих формулировках, и в примерах.
- `6157686` — `Уточнить порядок правил про пробелы`.
  Аккордеон `Пробелы` переименован в `Обычные пробелы` и перенесен после `Неразрывные пробелы`, чтобы было понятно, почему в правилах есть две темы про пробелы.
- `0a48e7b` — `Вынести контент UI в отдельный файл`.
  Тексты всех вкладок перенесены в `src/ui-content.js`, а `scripts/sync-ui-content.js` синхронизирует их с `src/ui.html` перед сборкой. Это позволяет править подписи, правила и текст вкладки `О плагине` без риска задеть TypeScript-ядро типографики. Проверять после таких правок нужно `npm run build` и локальный предпросмотр UI.
- `f242a08` — `Исправить неразрывные пробелы после дат и знаков`.
  Неразрывный пробел больше не ставится после дат формата `9.05`, `09.05`, `9.05.2026`, `09.05.2026`, если дальше идет слово. После `№` и `§` неразрывный пробел ставится только перед цифрой, поэтому случаи вроде `№,` не ломаются. Проверено на режиме `Для разработки`: лишние `*` после дат и после `№,` не появляются.
- `862d5d5` — `Переименовать команды плагина`.
  В `manifest.json` команды меню переименованы: быстрый запуск теперь называется `Запустить типограф`, а команда открытия интерфейса — `⚙️ Настройки`. Внутренние command id не менялись: `run-chistovik` остается быстрым запуском без UI, `open-settings` открывает окно настроек.
- `0f7d730` — `Ускорить запуск плагина на больших файлах`.
  В `manifest.json` добавлен `documentAccess: "dynamic-page"`, чтобы Figma не загружала все страницы файла перед запуском. Быстрый запуск теперь явно грузит только текущую страницу, ищет текстовые слои через `findAllWithCriteria({ types: ["TEXT"] })` и пропускает скрытые слои по умолчанию. В ядро добавлен флаг `processHiddenNodes: false` для будущего чекбокса `Обработать скрытые слои`. Вместе с пропуском скрытых слоев изменились уведомления: теперь они различают пропущенные замочки, скрытые слои и одновременный пропуск замочков со скрытыми слоями.
- `72ce308` — `Задокументировать уведомления плагина`.
  В README добавлен полный список текущих уведомлений, включая варианты с пропущенными замочками, скрытыми слоями и одновременным пропуском двух типов слоев. Также зафиксировано правило: перед коммитом нужно писать пользователю полный список изменений, которые попадут в коммит.
