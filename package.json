{
  "name": "chistovik-figma-plugin",
  "version": "1.0.0",
  "private": true,
  "description": "Figma plugin for Russian typography cleanup.",
  "scripts": {
    "sync-ui": "node scripts/sync-ui-content.js",
    "dev": "npm run sync-ui && tsc -p tsconfig.json --watch",
    "build": "npm run sync-ui && tsc -p tsconfig.json",
    "test": "npm run build && node scripts/test-clean-typography.js"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.106.0",
    "typescript": "^5.6.3"
  }
}
